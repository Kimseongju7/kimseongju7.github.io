---
title: 'AI Infra 교육 정리본'
date: 2026-07-08 00:00:00 +0900
tags: [aws, sagemaker, 머신러닝, mlops, swm]
categories: [학습, aws]
description: 'SWM AI Infra 교육 1~2일차 정리본. 머신러닝 기초 개념부터 데이터 준비, 모델 훈련·평가·배포, 보안, MLOps/CI-CD 파이프라인까지 Amazon SageMaker 중심으로 다룬다.'
---
SWM AI Infra 교육 1~2일차 정리본. 머신러닝 기초 개념부터 데이터 준비, 모델 훈련·평가·배포, 보안, MLOps/CI-CD 파이프라인까지 Amazon SageMaker 중심으로 다룬다.

---

## 1일차: 머신러닝과 AI 인프라 기초

### 과정 개요
5일간 진행되는 과정으로, 앞의 3일은 "머신러닝 엔지니어링 온 AWS", 목·금 이틀은 "디벨로핑 제네릭 AI 애플리케이션(Developing Generative AI Applications)"을 다룬다. 테크니컬 인스트럭터 김현규 강사가 과정을 총괄하며, 강의는 45~50분 진행 후 10~15분 휴식하는 방식으로 운영된다. 참가자들은 컴퓨터공학·인공지능·통계학·자율전공 등 다양한 전공 출신이며, AWS 사용 경험은 처음인 인원과 학교 수업·프로젝트로 경험해본 인원이 절반씩이다. 대부분 파이썬 코드 작성·배포 경험이 있고, 업스테이지 LLM API를 활용한 추론 실습 경험이 있는 인원도 확인되었다. 검증·최적화에 대해 약 3분의 1은 모른다고 응답했고 나머지는 이해도가 있으나, 실제 구현 경험자는 1명뿐이었다. 반면 머신러닝·딥러닝·생성형 AI·에이전트 AI의 차이는 대체로 잘 이해하고 있었고, RAG 이해도는 높은 편이나 에이전트 워크플로우·시스템 이해도는 절반 정도였다.

교육 콘텐츠는 총 11개 모듈로 구성되며, 모듈 1(머신러닝 기초) → 모듈 2(비즈니스 문제의 머신러닝 매핑) → 모듈 3·4(데이터 준비) → 모듈 5·6(모델 훈련) → 모듈 7(모델 평가·튜닝) → 모델 배포·보안 → MLOps·모델 모니터링 순으로 진행된다. 진행 방식은 설문을 통해 "이론적 공백을 최소화하며 핵심 위주로 압축 진행하고, 수요일에 별도 워크숍/실습 세션을 추가"하는 방향으로 확정되었다. 1일차 목표는 모듈 5(가능하면 6)까지, 2일차까지 11개 모듈을 모두 마치는 것이다.

### 머신러닝과 AI 기초 개념
일반적인 프로그램은 개발자가 과거 데이터를 분석해 규칙을 도출하고 이를 코드로 구현하는 방식으로 동작한다. 반면 머신러닝은 과거 데이터와 알고리즘을 기계에 입력해 데이터 안의 패턴과 규칙을 기계가 스스로 학습하게 하고, 학습된 모델에 새 데이터를 넣어 미래 값을 예측한다. 인공지능 개념 자체는 1950년대부터 연구되었으며, 초기에는 인간이 모든 규칙을 직접 코딩하는 규칙 기반 시스템(예: 초기 자율주행)이 쓰였다.

이후 데이터와 컴퓨팅 파워가 늘면서 딥러닝 시대가 열려 비선형 데이터까지 추론할 수 있게 되었고(알파고 등), 트랜스포머·어텐션 메커니즘의 발전으로 콘텐츠를 직접 생성하는 생성형 AI 시대로 이어졌다. 딥러닝은 입력층·은닉층·출력층으로 구성되며, 각 은닉층·출력층에 활성화 함수를 적용해 비선형 문제를 해결한다는 점이 전통적 머신러닝과의 핵심 차이다. 컴퓨터 비전에서는 이미지 전체 범위에서 시작해 점차 모서리·라인 같은 세부 특징을 추출하는 방식으로 학습이 진행된다.

머신러닝 학습 방식은 지도학습·비지도학습·강화학습 세 가지로 나뉜다. 지도학습은 정답 라벨을 붙인 데이터로 기계를 학습시키는 방식(예: 이미지에 고양이/강아지 라벨 부착)이고, 비지도학습은 라벨 없는 대량 데이터에서 기계가 스스로 패턴을 찾는 방식(더 많은 데이터·자원·시간이 필요하며 결과 모델도 더 복잡)이다. 강화학습은 행동 결과에 보상/페널티를 부여해 보상을 최대화하는 방향으로 학습시키며, 자율주행이 대표 사례다.

지도학습은 다시 분류(범주를 나누는 작업)와 회귀(연료 소비량, 전기세, 칼로리, 임금 등 수치를 예측)로 나뉘며, 대표 알고리즘으로 그레이디언트 부스팅·XGBoost·의사결정나무·서포트 벡터 머신·최근접 이웃·선형 회귀 등이 있다. 비지도학습의 대표 기법은 데이터를 그룹화하는 클러스터링(K-means 등)과, 변수 간 관계를 파악해 추천에 활용하는 연관 분석(예: 김밥 구매 고객에게 김치 추천)이다. 차원 축소는 데이터의 차원(변수 개수)을 줄여 훈련 속도를 높이고 오차를 최소화하는 기법이다.

머신러닝 파이프라인 전체 흐름은 데이터 수집 → 탐색(이상치·노이즈 확인) → 전처리(정제·변환) → 훈련/검증/테스트 분할 → 알고리즘 선택 및 훈련 → 성능 평가 → 하이퍼파라미터 튜닝 → 목표 오차 도달 시 컨테이너 형태로 패키징 → 배포(엔드포인트 생성) → 배포 후 지속적인 성능 모니터링 및 재조정 순으로 이어진다. 애플리케이션은 프론트엔드·백엔드·데이터베이스로 구성되며, 사용자가 UI로 요청하면 백엔드가 모델에 전달하고 추론 결과를 받아 최종 응답을 제공한다.

### 머신러닝 프로젝트의 조직과 역할
- **데이터 과학자**: 데이터 수집·전처리·피처 엔지니어링·모델 구축 및 훈련을 담당한다.
- **데이터 엔지니어**: 데이터 과학자가 업무를 수행할 수 있도록 스파크·파이프라인 등 기반 인프라를 구축한다.
- **MLOps 엔지니어**: 모델 배포·운영·유지 관리를 담당하며 AWS CloudWatch 등으로 시스템을 모니터링하고, 쿠버네티스 등 배포 환경과 ML 워크플로우 지원 솔루션을 관리한다.
- **머신러닝 엔지니어**: 위 인프라·워크플로우를 포함한 머신러닝 시스템 전반을 총괄 관리한다.

### AWS AI 스택과 SageMaker
AWS의 AI 스택은 인프라·플랫폼·서비스 세 범주로 나뉜다. 인프라는 머신러닝·생성형 AI·에이전틱 AI 파이프라인 수행에 필요한 컴퓨팅·스토리지 자원(GPU 가상 서버, NVIDIA GPU나 AWS 자체 칩 인스턴스 등)을 제공한다. 플랫폼(SageMaker AI 등)은 모델 개발부터 배포, MLOps, 거버넌스, 데이터 전처리까지 엔드투엔드 개발 인프라를 지원한다. 서비스는 이미 모델까지 구축되어 즉시 도입 가능한 형태로, Amazon Translate·Polly·Transcribe(번역/음성), 아마존 커넥트(고객센터 AI), 퀵사이트 등이 있으며, 컴퓨터 비전·문서 이미지 추출·추천 엔진·사기 탐지 모델 등에 활용된다. 개발 여력이 부족할 때 이런 서비스형 모델을 조합해 시간과 비용을 절감할 수 있다(예: 클로드 코드 위스퍼러 사례).

SageMaker AI는 데이터 준비/전처리, 특성(피처) 중앙 관리, 노트북 등 개발 환경, 모델 훈련(사전 훈련된 모델·내장 알고리즘 지원으로 PyTorch/TensorFlow를 직접 구축할 필요 없음), 하이퍼팟(HyperPod) 클러스터, 오토ML(코드 없이 모델 훈련), 튜닝, 배치 추론, 모델 편향 감지/테스트, 데이터 랭글러, 디버깅 도구, 실험 관리, 데이터 레이블링까지 머신러닝 엔드투엔드 개발에 필요한 기능을 통합 제공한다. 자체적으로 머신러닝을 구현하면 프레임워크뿐 아니라 인프라·스토리지·파이프라인까지 직접 구축해야 하는 번거로움이 있다.

### Responsible AI
AI가 가짜 뉴스·딥페이크 생성(성범죄물 제작·유통 포함)에 악용되는 사례가 늘면서(예: 미국 대선 기간 GPT를 이용한 가짜 뉴스 생성 사례) 책임 있는 AI(Responsible AI)의 중요성이 커지고 있다. 기술 발전 속도에 비해 정책·법률 대응이 지연되는 가운데, 대한민국은 세계 최초로 AI 기본법을 제정·시행했으며 실질적 도입 시점은 EU AI 법안보다도 앞선다. AI 기본법은 AI 개발·서비스 제공 사업자에 대한 지원과 규제를 목적으로 하며, 의료·체육·대출·교통·공공 서비스·교육 분야는 "영향 AI"로 분류되어 더 엄격한 관리를 받는다. 콘텐츠 플랫폼에서는 AI 생성 콘텐츠임을 사용자가 즉시 식별할 수 있도록 표시하는 사례도 늘고 있다.

기존 애플리케이션은 조건문·반복문으로 결정론적 결과를 내지만, AI 모델은 확률 기반으로 콘텐츠를 생성·판단하므로 비결정론적이며 프롬프트 인젝션 등 새로운 유형의 보안 위협에 노출된다. 이 때문에 Responsible AI 도입이 필수적이며, 핵심 구성 요소로 공정성·설명 가능성·투명성·보안·진실성·거버넌스·안전성·제어 가능성이 꼽힌다. 설명 가능성은 모델이 특정 결정을 내린 이유를 명확히 설명할 수 있는 능력을 의미하는데, 변수 차원이 낮은 단순 모델은 추론이 쉽지만 수천억 파라미터의 LLM 등 딥러닝/생성형 AI 모델은 해석이 어렵다. 의학·법률처럼 의사결정이 중요한 분야일수록 Responsible AI 도입이 확대되고 있으며, 이는 데이터 부족 문제 해결과 모델 편향성 완화에도 기여한다.

### 머신러닝 비즈니스 과제 선정 기준
머신러닝 도입 여부는 다음 기준으로 판단한다: ①데이터 안에 패턴/관계가 존재하는지, ②충분한 데이터가 확보되어 있는지(일반적으로 특성 개수의 10배 이상 데이터 필요), ③지속적인 학습이 필요한 문제인지, ④성과를 수치로 정의할 수 있는지(예: 고객 이탈률 10% 감소, 사기 탐지율 25% 상승), ⑤일반 애플리케이션·기존 솔루션 대비 실제로 더 나은지. 간단한 알고리즘으로 해결 가능한 문제라면 굳이 머신러닝을 도입할 필요가 없으며, 머신러닝 모델 개발은 생성형 AI만큼은 아니어도 상당한 시간·비용이 든다.

다뤄진 사례:
- **이커머스**: 계정 도용·결제 사기·허위 제품 등록 문제 해결. 계정 손상률 감소와 허위 등록 제품 제거를 목표로, 신뢰도 개선과 손실 감소를 성공 지표로 설정(이 과제와 관련해 8,600이라는 수치가 언급됨).
- **마케팅**: 연령·위치·성별·직업 등 CSV 데이터로 소득 수준을 예측해 매출 증대. 소득과 상관관계가 낮은 성별·인종 등은 특성에서 제외 검토.
- **비즈니스 도서관**: 개인화 마케팅 콘텐츠 생성, 콘텐츠 제작, 실시간 감정 분석, 구매 패턴 예측 네 가지 과제 중 구매 패턴(구매 여부 예측) 분석이 머신러닝 적용 핵심 분야로 선정, 매출 목표 20% 달성 전략과 연계.
- 변화하는 트렌드(러닝, 트레일 러닝, 하이록스 등)에 맞춘 개인화 추천처럼 전통적 앱 개발보다 머신러닝 재훈련/튜닝이 효율적인 경우도 논의됨.
- 참고 학습 자료로 생활코딩의 머신러닝 과정이 소개됨(소비 패턴 분석 절약 기능, 해외 주식 변동성 분석, 이미지 유사 상품 추천 등 아이디어 사례 포함).

### 데이터 준비: 저장소와 형식
데이터베이스는 애플리케이션의 읽기/쓰기 트랜잭션(OLTP) 처리용 저장 공간이고, 데이터 웨어하우스·데이터 레이크는 분석·활용(OLAP) 목적의 저장 공간이다. 데이터 레이크는 문서·PDF 등 가공되지 않은 원시(로우) 데이터를 저장하며, 데이터 웨어하우스는 정제·변환된 데이터(정형 데이터 위주, 반정형 데이터도 일부 포함)를 저장한다. 데이터 마트(특정 목적용 별도 저장소)는 최근 실무에서 활용 빈도가 낮아지는 추세다. 지도학습(분류) 문제의 경우 고객 데이터는 보통 데이터베이스/데이터 웨어하우스에서, 컴퓨터 비전(제품 결함 판별용 사진 등)은 데이터 레이크에서 소스를 가져온다.

ETL(Extract-Transform-Load)은 정형·반정형 데이터를 추출·변환·적재하는 처리 과정이다. 데이터 품질의 핵심 기준은 정확성(오류 없음), 완전성(중요 변수 누락 없음), 일관성(단위·형식 통일, 예: 달러/원화/엔화 혼용 방지), 최신성(실시간 예측엔 최신 데이터 필요), 대표성(문제 해결에 유효한 특성만 선별)이다.

데이터 유형은 자연어(NLP: 감정분석·챗봇·문서요약), 테이블(분류·회귀), 시계열(주가·부동산 예측 등 타임스탬프 포함) 등으로 나뉘고, 정형/비정형/반정형으로도 분류된다. 저장 포맷은 크게 행 기반, 열 기반, 객체 표기법(JSON 등)으로 나뉜다. 행 기반 포맷은 데이터 수집·ETL 처리에 적합하고, 열 기반 포맷은 특정 열만 선택적으로 읽을 수 있어 모델 훈련·피처 엔지니어링에 유리하며 압축 효율도 더 높다(ORC가 Parquet보다 하이브 환경에서 효율적). Avro는 바이너리+스키마 구조의 직렬화 포맷이며, SageMaker의 RecordIO는 데이터를 레코드 단위 청크로 나눠 바이너리로 저장하는 훈련 데이터 포맷이다. 실무에서는 CSV 포맷이 여전히 많이 쓰이며, SageMaker 알고리즘별로 요구 포맷이 다르므로(예: 일반 분류는 CSV, 이미지 분류는 RecordIO/이미지 파일, NLP 시퀀스 모델링은 RecordIO, TensorFlow 텍스트 분류는 CSV/텍스트) 공식 문서에서 확인이 필요하다. 데이터 수집 방식도 주기적인 배치와 실시간 스트리밍(Kafka, Kinesis)으로 구분된다.

스토리지 옵션은 다음과 같이 선택한다: **S3**는 인터넷 기반 객체 스토리지로 무제한 저장이 가능해 데이터 레이크·모델 아티팩트 저장에 쓰이며 HTTPS로 GET/PUT 방식 입출력을 한다. **EBS**는 EC2에 연결하는 SSD/HDD 드라이브다. **EFS**는 NAS와 유사한 네트워크 공유 파일 스토리지로 여러 컴퓨팅 인스턴스 간 데이터셋·훈련 코드·모델 아티팩트 공유 및 분산 훈련에 적합하다. **FSx for Lustre**는 입출력 속도가 빠르고 지연이 짧아 대용량 데이터셋 훈련이나 실시간 예측에 적합하지만 장애 시 데이터 손실 위험이 있어 평소엔 S3에 데이터를 두고 훈련 시에만 Lustre로 옮겨 사용한다. SageMaker는 데이터셋이 EFS에 있으면 EFS를, 50~100GB 미만이면 S3 파일 모듈을, 50GB를 초과하면 개별 파일이 50MB 미만인지 등을 확인해 적절한 옵션을 선택하도록 제한한다.

### 데이터 탐색과 시각화
데이터 분석 시 볼륨·유형 파악 후 시각화를 통해 분포와 이상치를 확인한다. 범주형 데이터는 막대 차트·파이 차트·히트맵으로, 수치형(연속형) 데이터는 히스토그램·산점도로 분포를 비교한다. 히스토그램은 데이터가 좌우로 치우쳤는지, 이상치가 있는지 수치 단위로 보여주며, 바이올린 플롯은 그룹 간(예: 도시별 소득 수준) 분포 형태를 비교하는 데 유용하다.

평균은 이상치에 왜곡되기 쉬워 분위수(1/4, 1/2, 3/4 지점)를 함께 확인한다. 박스 플롯(상자 그림)에서 중앙선은 중앙값(50%), 상자 하단/상단 모서리는 각각 25%/75% 분위수를 나타내고, 상자 밖 수염(whisker)을 벗어난 값이 이상치다. 이상치는 근거가 있는 경우(예: 특정 지역의 소득 수준)도 있어 훈련 데이터 포함 여부를 신중히 판단해야 한다.

변수 간 관계는 산점도(scatter plot)나 다변량 통계로 파악하며, 상관관계 행렬과 열지도(heatmap)로 특성 간 연관 강도·방향성을 시각화한다(보라색=음의 상관관계, 녹색=양의 상관관계, 1에 가까울수록 강한 상관관계, 예: 몸무게-비만도 상관계수 0.7~0.8). 상관관계가 지나치게 높은 두 변수는 데이터 중복성과 오버피팅을 막기 위해 하나를 특성에서 제외하기도 한다.

### 데이터 클리닝과 피처 엔지니어링
데이터 클리닝은 값 변환, 형식 수정, 이상치·중복 항목 제외, 누락 데이터 보완 등을 포함한다. 결측치 유형은 세 가지다: **완전 무작위 누락**(다른 변수와 무관하게 발생, 전산 오류 등 관련성 없는 경우 행/열 삭제로 처리), **비무작위 누락**(특정 집단이 응답을 회피해 발생, 가장 해결하기 어려움 — 예: 고소득층의 소득 응답 거부), **편향 유발 누락**(예: 특정 성별이 몸무게 기재를 꺼리는 경향). 처리 방법은 성별처럼 편향 유발 변수를 제거하거나, 키·나이 등 다른 변수로 값을 추정/대체하거나, 전문가 지식이나 유사 직군·지역 데이터를 참조해 값을 생성하는 것이다.

피처 엔지니어링은 원시 데이터를 계산·가공해 훈련에 의미 있는 특성(변수)을 만드는 과정이다. 범주형 데이터는 순서 유무에 따라 인코딩 방식이 달라진다.
- **레이블 인코딩**: 범주에 0부터 순차적인 숫자를 부여. 숫자 크기·순서가 잘못된 영향을 줄 수 있다는 한계가 있다.
- **원-핫 인코딩**: N개 범주를 N개의 비트 벡터(칼럼)로 분리해 해당 시 1, 아니면 0으로 표시. 선형 회귀·신경망처럼 수치 크기가 성능에 영향을 주는 모델에서 오인을 방지하지만, 범주가 늘수록 차원이 증가해 "차원의 저주" 문제가 생긴다.
- **순서형 인코딩**: 학력처럼 순서가 있는 데이터에 스몰(0)·미디움(1)·라지(2)처럼 순차적 수치를 부여해 순서 정보를 보존한다.
- **바이너리 인코딩**: 범주를 이진 코드로 변환(예: 레드/블루/그린/옐로우 4개 범주를 0과 1 조합으로 표현). 범주가 많을 때 오버랩으로 학습이 가능하다.

수치형 데이터는 스케일링으로 처리한다. **정규화**는 값을 0~1 또는 -1~1 범위로 제한하며 신경망·거리 기반 알고리즘에서 이상치 영향을 줄인다. **표준화**는 평균 0, 표준편차 1로 조정해 정규분포에 가깝게 만들며 선형/로지스틱 회귀 등 선형 모델에 적합하다(정규분포가 아닌 데이터에 쓰면 왜곡 가능). **비닝**은 연속형 데이터를 구간별 범주로 나눠(예: 점수 구간을 등급으로) 훈련 시간을 줄이지만 정보 손실이 있을 수 있다. 로그 변환 등으로 데이터 범위를 축소하기도 한다.

차원 축소는 상관관계가 낮은 특성을 제거하거나(예: 부동산 가격 예측에서 위치·등록일 등 4개 특성 중 상관 낮은 특성 제거로 4차원→2차원), **주성분 분석(PCA)**처럼 변수 간 연관 관계를 분석해 정보 손실을 최소화하며 차원을 줄이는 방식이 있다. PCA는 데이터 평균을 원점으로 이동(시프팅)한 뒤, 원점을 지나는 직선까지의 수선의 발 거리 제곱합(Sum of Square)이 최대가 되는 직선(PC1)을 찾고, PC1 기준으로 데이터를 통합해 차원을 줄인다(이 과정에서 정보 손실은 불가피). 예를 들어 주택 가격 예측 시 면적·침실·욕실 등 변수를 "크기"라는 하나의 차원으로, 가격·세율·주별 세금 체계 등 6개 변수를 2개 차원으로 통합할 수 있다.

이런 전처리는 SageMaker Data Wrangler(GUI 클릭만으로 전처리, 커스텀 코드 추가 가능)로 코딩 없이 수행하거나, SageMaker Processing(관리형 스크립트 실행) 또는 Amazon EMR(오픈소스 기반 대규모 ETL·빅데이터 처리, 하둡 에코시스템 활용 시)로 처리할 수 있다.

### 모델 훈련 기초
SageMaker에서 모델을 구현하는 방법은 크게 네 가지다: ①SageMaker 내장 알고리즘이 포함된 도커 이미지 사용(가장 간단), ②TensorFlow/PyTorch 등 프레임워크가 포함된 컨테이너에 사용자 스크립트만 추가, ③완전한 커스텀 컨테이너 직접 구성, ④마켓플레이스의 서드파티 알고리즘 사용. 알고리즘 선택 시 문제 유형(분류/회귀/클러스터링 등), 요구 데이터 포맷, 필요한 CPU/GPU 리소스와 소요 시간, 전통적 머신러닝 vs 딥러닝 여부를 함께 고려해야 한다. 정형 데이터 기반 지도학습(CSV 등)은 보통 CPU로 충분하지만, 비정형 데이터(네트워크·트랜스포머 등)는 GPU·분산 컴퓨팅이 필요해 비용이 커진다. SageMaker는 사용량 기반 과금 구조상 비용 부담이 크다는 지적이 많아, 세이빙스 플랜과 스팟 인스턴스로 비용을 절감할 수 있다.

지도학습 알고리즘(그레이디언트 부스팅, XGBoost, KNN, 선형 학습자, 결정 나무, SVM 등)과 비지도학습 알고리즘(랜덤 포레스트, IP 인사이트, K-평균, PCA, 이상 탐지 등), 텍스트/음성용 시퀀스 모델·토픽 모델링 등 다양한 카테고리의 내장 알고리즘이 제공된다. 임베딩을 통해 데이터를 벡터화해 RAG 시스템 등에 활용할 수도 있다.

**XGBoost 동작 원리**: 1차 훈련 모델의 예측값과 실제 레이블 간 오차를 계산하고, 이 오차를 줄이는 방향으로 트리를 반복 추가해 최종 모델을 만드는 부스팅 알고리즘이다. Decision Tree를 개선한 방식으로, 데이터를 분할할 때마다 유선(gain) 점수를 계산한다. 예시: 정규화 파라미터 알파를 0으로 두면 오차 제곱합을 오차 개수로 나눈 유선 점수가 16으로 계산되고, 데이터를 좌/중/우로 분할해 각 영역의 유선 점수를 비교했을 때 가장 오른쪽 영역(전체의 약 71%)에서 가장 높은 이득이 나와 해당 방향으로 분할, 이를 기반으로 두 번째 모델을 만들어 첫 번째 모델의 오차를 보정한다. 이 과정을 더 이상 이득을 얻을 수 없거나 데이터가 소진될 때까지 반복한다.

**경사하강법**은 손실 함수의 기울기가 0이 될 때까지 가중치를 반복 업데이트하는 최적화 기법이다. 훈련 데이터셋을 한 번 모두 학습하는 단위를 에포크라 하며, 방식은 세 가지다: **배치 경사하강법**(전체 데이터로 1에포크 학습 후 가중치 업데이트 — 정확하지만 느림), **확률적 경사하강법**(샘플 하나마다 업데이트, 예: 1,000개 데이터셋이면 1,000번 업데이트 — 빠르지만 노이즈에 취약), **미니배치 경사하강법**(데이터를 일정 크기 배치로 나눠 배치 완료 시마다 업데이트, 예: 1,000개를 100개씩 10배치로 나누면 10번 배치 완료가 1에포크 — 둘의 장단점을 절충).

손실 함수는 예측값과 실제값 차이를 계산하는 지표로, 회귀 모델은 평균 제곱근 오차(RMSE) 등을, 로지스틱 회귀 등 분류 모델은 로그 가능도(log-likelihood)를 사용한다. 로지스틱 회귀는 특정 기준 점수로 결과를 0 또는 1로 분류하는 모델이며, 최적화에는 경사하강법을 쓴다.

**과대적합/과소적합**: 과소적합은 모델이 훈련 데이터의 패턴을 충분히 포착하지 못한 상태다. 과적합은 훈련 데이터에 과도하게 튜닝되어 노이즈까지 학습해 일반화 성능(검증 데이터 성능)이 떨어지는 상태로, 훈련 데이터의 다양성/볼륨을 늘리거나 정규화, 모델 복잡도 감소로 해결한다. 과소적합은 반대로 모델 복잡도를 높여 개선한다. 이는 편향-분산 트레이드오프로도 설명되는데, 과적합은 편향은 낮지만 분산이 높은 상태, 과소적합은 분산은 낮지만 편향이 높은 상태이며 최적 모델은 이 둘의 균형점을 찾는다.

데이터 분할 비율은 1일차에는 훈련용 60~70% / 검증용 10~20% / 테스트용 10% 기준이, 2일차에는 6:2:2, 7:2:1, 8:1:1(훈련:검증:테스트) 등의 비율이 언급되었다. 훈련 데이터로 모델을 학습시키고, 검증 데이터로 성능을 평가하며 하이퍼파라미터를 튜닝하고, 최종적으로 테스트 데이터로 배포 전 마지막 평가를 수행한다. 훈련은 알고리즘과 데이터를 결합해 모델이 특성별 가중치를 스스로 학습하는 과정이다.

실무에서는 SageMaker Python SDK로 Estimator 객체를 선언(권한 Role, 인스턴스 개수·유형 지정)하고 image_uri 모듈로 원하는 버전의 알고리즘(예: XGBoost) 컨테이너 이미지를 불러온 뒤, S3의 훈련/검증 데이터 경로를 지정해 훈련(fit)을 실행한다. 훈련 작업은 AWS 관리형 EC2 클러스터 인스턴스에서 컨테이너 기반으로 실행되며, 완료된 모델(가중치·성능 지표 포함)은 S3에 저장된다. 코딩이 부담스러운 사용자를 위해 **SageMaker Autopilot**(오토ML 기반 노코드 모델 구축, 이미지/텍스트/테이블 등 다양한 데이터 지원)과 **SageMaker Canvas**(코딩 없는 모델링, 퀵 모드는 프로토타입용으로 데이터셋이 작으면 약 30분, 스탠다드 모드는 프로덕션용으로 2~5시간 소요)도 제공된다.

모델 선택 시 해석 가능성과 비용의 트레이드오프를 고려해야 한다. 교육·의료처럼 고위험 영역은 해석 가능성이 필수적이며, 단순 선형 회귀는 해석이 쉽지만 SVM·신경망 등 복잡한 모델은 해석이 어려운 대신 비선형 문제 예측 정확도가 높다.

### 1일차 실습 안내
- **실습 1**: SageMaker Data Wrangler(SageMaker Canvas 통해 접속)를 활용한 GUI 기반 데이터 전처리, 또는 EMR 클러스터 + Spark 엔진을 활용한 데이터 전처리.
- **실습 2**: SageMaker Processing과 제공된 파이썬 코드로 데이터 전처리. 실습 1번은 약 1시간 15분, 실습 1·2번을 합친 전체 실습 시간은 쉬는 시간 포함 1시간 30분 내외였으며(실습 2번 소요 시간이 길어 1·2번을 병행 진행), 이후 실습 1·2번을 통합해 총 1시간 40분으로 배정된 회차도 있었다.
- 실습 환경은 버지니아 북부(us-east-1) 리전에 구성되며, 교육 종료(수요일) 이후에도 4주간 이용 가능하다. 클릭 위주 실습만으로는 학습 효과가 낮으므로 별도로 공유된 학습 노트(단계별 의미와 코드 동작 원리 설명 포함)를 함께 참고하도록 안내되었다.
- 1일차 목표는 총 11개의 노트를 완료하는 것이었고, 다음 날 실습을 위해 당일 교육 내용을 바탕으로 노트를 작성할 예정이었으며 새로 합류한 교육생에게는 별도 과제가 부여됐다. 2일차 후반부 기준 남은 교육 과정은 이론 8회·실습 4회, 총 6시간 정도로 예상됐다.

---

## 2일차: 모델 평가, 배포, 보안, MLOps

### 하이퍼파라미터와 모델 훈련 환경
하이퍼파라미터는 모델·알고리즘마다 설정값이 다르며, 학습률·에포크·미니배치 크기 등 공통 하이퍼파라미터는 실습 훈련 코드 실행 시 직접 확인한다. 일반적인 머신러닝·전처리·피처 엔지니어링에는 CPU 인스턴스를, 딥러닝·AI 모델 개발에는 GPU 인스턴스를 사용한다. **모델 레지스트리**는 모델 그룹을 만들어 여러 버전을 중앙에서 관리하며, 조직 차원의 모델 거버넌스(정책·프로세스 통합 관리)를 구현하는 데 쓰인다.

### 모델 평가 지표
분류 모델 평가는 **혼동 행렬(Confusion Matrix)**에서 시작한다. 참(True)은 예측과 실제 값이 일치하는 경우, 거짓(False)은 불일치하는 경우다. **정확도(Accuracy)**는 전체 중 올바르게 예측한 비율이지만, 데이터 불균형이 심하면(예: 전체 2,470개 샘플 중 긍정이 1,620개) 왜곡될 수 있어 다른 지표와 함께 봐야 한다. **정밀도(Precision)**는 긍정으로 예측한 것 중 실제 긍정 비율로, 스팸 메일 분류처럼 긍정 예측(거짓 긍정, False Positive 최소화)이 중요한 경우에 활용한다. **재현율(Recall)**은 암 진단처럼 거짓 부정(False Negative)을 최소화해야 하는 경우에 중요하다. **F1-Score**는 정밀도와 재현율의 조화 평균으로, 산업 현장에서 종합 성능 지표로 널리 쓰인다.

이진 분류 모델은 참/거짓을 직접 출력하는 게 아니라 참일 확률을 계산하며, 이 확률에 임계값을 설정해 참/거짓으로 최종 분류한다. 임계값이 낮아질수록 참 긍정 비율(TPR)과 거짓 긍정 비율(FPR)이 함께 상승한다. **ROC 곡선**은 FPR(x축)과 TPR(y축)로 임계값 변화에 따른 성능을 시각화하며, 곡선이 좌측 상단에 가까울수록 좋은 모델이다. **AUC**는 ROC 곡선 아래 면적으로, 값이 클수록(1에 가까울수록) 모델 성능이 우수함을 의미한다.

### 하이퍼파라미터 튜닝과 분산 훈련
알고리즘 튜닝 기법으로 **그리드 서치**(가능한 모든 조합을 학습), **랜덤 서치**, **베이지안 최적화**(이전 결과를 바탕으로 다음 값을 예측), **하이퍼밴드**(중간 체크포인트로 성능 좋은 세트에 자원 집중, 예: 80개 하이퍼파라미터 세트 중 우수한 세트에 자원 집중)가 있다. 훈련 횟수가 늘수록 시간·비용도 비례해 늘어나므로 효율적인 자원 투자가 필요하다. 튜닝 성능은 검증 데이터의 AUC 값으로 측정한다.

**분산 훈련**은 메모리·컴퓨팅 리소스를 분산해 훈련 시간을 단축하며, 모델 병렬화(가중치를 분할해 훈련 후 결합), 데이터 병렬화(대용량 데이터를 나눠 학습), 하이브리드(둘의 결합) 방식이 있다. SageMaker의 **HyperPod**는 컴퓨팅 클러스터를 생성해 분산 처리를 지원하며, 수십억 파라미터·대용량 이미지 데이터 처리 시 훈련 시간을 단축한다.

### 모델 배포 전략과 추론 방식
빌드된 모델 아티팩트는 S3에 저장되고, 이를 참조하는 SageMaker 엔드포인트를 생성해 클라이언트가 API(보통 API 게이트웨이 경유)로 추론을 요청한다. 배포 전략은 일반 애플리케이션 배포 전략과 크게 다르지 않다.
- **인플레이스/롤링 배포**: 기존 환경 내에서 모델을 교체하며, 롤링은 여러 인스턴스를 하나씩 순차적으로 교체하는 방식이다. 속도가 빠르고 비용 효율적이지만 문제 발생 시 롤백이 번거롭다.
- **블루그린 배포**: 기존 환경(블루)과 새 환경(그린)을 동시에 운영하다 라우팅을 전환한다. 문제 발생 시 이전 버전으로 즉시 되돌릴 수 있어 안전하지만, 환경을 미리 구축해야 해 상대적으로 느리고 환경 격리 수준이 높다.
- **선형(카나리) 배포**: 트래픽을 일부만 신규 환경으로 보내 검증 후 점진적으로 확대하는 방식으로(예: 5분마다 20%씩 전환), 가장 안전하지만 배포 속도가 느리다.

추론 방식은 네 가지다.
- **실시간 추론**: EC2 기반 엔드포인트를 상시 구성해 요청에 즉시 응답. 24시간 상시 실행.
- **서버리스 추론**: AWS Lambda(런타임+라이브러리+코드로 구성된 함수 단위) 기반. 평소엔 비활성 상태이다가 요청 시 마이크로 VM 환경을 생성해 처리하며, 이 지연을 **콜드 스타트**라 한다(15분간 요청이 없으면 환경 자동 소멸). 인프라 관리 부담이 없지만 콜드 스타트 지연이 있으며 CPU 기반 추론만 지원한다.
- **비동기식 추론**: 요청을 대기열에 저장해 처리, 요청량이 많거나 처리 시간이 긴 작업에 적합.
- **배치 변환**: 실시간 응답이 필요 없는 데이터 분석·인사이트 도출·관리적 목적의 대량 추론에 사용, 비용 절감 효과가 있다.

추론 인프라 구축 시 EKS·ECS·Lambda 등 AWS 컴퓨팅 서비스 중 선택하며, 관리형 서비스(SageMaker 엔드포인트)는 운영 편의성이 높은 대신 커스터마이징이 제한적이고, EKS(쿠버네티스)는 서드파티 도구 연동 등 커스터마이징 자유도가 높은 대신 관리 포인트가 늘어나는 트레이드오프가 있다.

**엔드포인트 생성 절차**는 ①S3 모델을 컨테이너 이미지와 연결해 모델 객체 생성 → ②`create_endpoint_config`로 엔드포인트 구성(구성 이름, 변수명과 참조 모델, 인스턴스 유형 지정) → ③엔드포인트 배포의 3단계로 진행된다. 구성 요소에는 리소스 오토스케일링 정책(클라이언트 호출량에 따라 자동 조절), 데이터 캡처, 로깅, 보안 제어, 배포 정책이 포함된다.

**다중 모델 엔드포인트**는 동일 프레임워크 기반의 여러 모델을 하나의 엔드포인트/인스턴스에서 운영해 자원 활용도를 높이며(A/B 테스트, 목적별 분리, 멀티 테넌트에 활용), 호출 시 API 파라미터에 엔드포인트 이름과 타겟 모델을 함께 명시해야 한다. **다중 컨테이너 엔드포인트**는 서로 다른 프레임워크로 전처리·추론·후처리를 하나의 엔드포인트에서 파이프라인처럼 순차 실행하며, 호출 시 타겟 컨테이너 호스트 네임을 지정한다. **Amazon SageMaker Inference Recommender**는 인스턴스 수량·컨테이너 파라미터·메모리 크기 등을 분석해 최적의 인스턴스 유형·구성을 추천, 성능과 비용을 동시에 최적화한다.

프로덕션 환경에서는 비용·성능보다 안전성·가용성을 우선해야 하며(서버리스보다 예측 가능한 안정적 구성 권장), 문제 발생 시 대응을 위해 자동 롤백 기능을 구성하는 것이 중요하다.

### 2일차 실습(실습 3~6)
- **실습 3**: 소득 데이터를 이용해 연봉 5만 달러 초과 여부를 예측하는 XGBoost 이진 분류 모델을 훈련. SageMaker Debugger로 성능 보고서를 생성해 레이블 분포·손실 비교 그래프·특성 중요도로 과적합 여부를 점검(훈련·검증 오차 격차 증가 시 과적합 의심, 훈련 시 FP32 정밀도 활용).
- **실습 4(랩4)**: XGBoost 하이퍼파라미터 튜닝 실습. 코드에서 하이퍼파라미터 레인지를 설정하면 SageMaker가 범위 내에서 자동으로 조합을 생성해 튜닝하며, 검증 데이터 AUC로 성능을 비교한다.
- **회귀 모델 실습**: 아이템 ID·상점 ID·시간대별 구매 정보·제품 가격·스코어 등이 담긴 CSV 데이터(2017년 10월 1일~2019년 12월 타임스탬프)로, 처음엔 향후 5년간 제품별 가격을 예측하려 했으나 예측 대상이 가격이 아닌 수요임을 정정해 향후 5개월간 아이템별 수요를 예측하는 회귀 모델을 구축했다. 학습 데이터는 20명 대상 27개월치 시계열 데이터로 단기 시계열에 최적화된 알고리즘을 사용했고, RMSE(Root Mean Square Error) 최소화가 목표였다. 모델 빌드 유형은 예측·테스트용 퀵 빌드(약 30분, 프로토타입), 커스터마이징이 필요한 커스터마이즈 빌드, 약 3~4시간 후 코딩·GUI 작업이 가능해지는 스탠더드 빌드로 구분됐다.
- **실습 5**: 블루그린 배포 + CloudWatch 기반 자동 롤백 실습. 서로 다른 특징(그중 하나는 의도적 오류 포함)을 가진 3개 모델을 준비해 배포 후 장애 상황을 시뮬레이션한다. 엔드포인트 구성을 모델 1→모델 2로 교체하며 트래픽을 20%씩 선형적으로 전환하고 각 단계마다 1분간 대기한다. CloudWatch로 SageMaker 엔드포인트 변형 지표를 60초 간격으로 수집하고, `Auto Rollback Alarm` 함수로 경고를 정의한다. API 500 에러가 60초 동안 1회 이상 발생해 1% 임계치를 초과하거나, 모델 레이턴시가 10ms 이상 지속되면 경고 상태로 전환되어 자동 롤백이 트리거된다.
- **실습 6**: 실무에서 API와 AI 기술을 프로젝트에 적용하는 방법을 익히는 실습(약 1시간 소요).

### 보안과 거버넌스
계정·인프라 보호의 핵심 요소는 인증과 권한, 네트워크, 인프라 구성, CI/CD 파이프라인 보안이다. AWS 계정 인증·권한 관리는 **IAM**으로 수행하며, 계정 이메일·비밀번호를 다른 서비스와 동일하게 쓰는 것은 보안상 위험하다. **MFA**를 설정하면 외부에서 탈취한 계정 정보만으로는 로그인할 수 없어 계정 보안이 크게 강화된다(해커가 로그인에 실패하면 코인 채굴 등 다른 방식의 공격을 시도할 가능성도 언급됨). 루트 사용자 보호가 최우선이며, 개발자·운영자·데이터 사이언티스트 등 역할별로 개별 IAM 사용자를 만들어 필요한 최소 권한만 부여해야 한다.

IAM 인증 방식은 관리 콘솔 로그인(사용자 이름·비밀번호)과 API 호출용 액세스 키(액세스 키 ID + 시크릿 액세스 키) 두 가지다. SDK 작업은 액세스 키만으로 가능하며, 콘솔 작업이 필요한 경우에만 별도 콘솔 접근 권한을 부여한다. 실습 환경은 AWS 빌더 ID 기반 SSO(싱글 사인온)로 자동 로그인되는 구조다. 권한 정책은 아이덴티티 기반 정책(사용자/역할에 부여)과 리소스 기반 정책(S3 버킷, API 게이트웨이 등 리소스에 직접 적용)으로 구분된다. 데이터 사이언티스트·MLOps 엔지니어·데이터 엔지니어·머신러닝 엔지니어 등 직무별로 SageMaker AI 사용 권한을 적절히 부여해야 한다.

SageMaker Studio는 기본적으로 인터넷을 통해 퍼블릭하게 통신하는데, 엔터프라이즈 환경에서는 보안을 위해 **VPC 온리 모드**로 설정해 특정 VPC·서브넷을 통해서만 트래픽이 흐르도록 제한하고, 프라이빗 링크로 VPC 내 통신을 구성한다. 이 경우 외부 패키지를 직접 수신할 수 없어 커스터머 VPC 내에 별도 게이트웨이를 구현하고 인터넷 게이트웨이와 연결해야 외부 통신이 가능하다. 네트워크 보안은 보안 그룹(인스턴스 단위 방화벽), 네트워크 ACL(서브넷 단위 방화벽), 네트워크 방화벽 세 가지를 함께 활용하며, 서브넷별 트래픽 흐름은 라우팅으로 제어한다. VPC Flow Logs로 트래픽을 캡처·분석할 수 있다.

데이터 암호화는 **Amazon KMS**의 암호화 키로 학습 데이터·애플리케이션 데이터·테스트 데이터·모델 아키텍처·공유 코드 등 파이프라인 전 구간에 적용해야 하며, 동일 키가 있어야만 복호화할 수 있다. 그 외에도 CloudWatch(지표 로그 수집·분석), Config(S3 암호화 여부 등 규정 준수 자동 검사), API 호출/응답 로그, EC2 취약점 탐지, SSH 무차별 대입 공격 등 지능형 공격 실시간 탐지 등의 보안 모니터링 체계가 필요하다.

### MLOps와 CI/CD 파이프라인
**CI/CD 파이프라인**은 애플리케이션의 개발-테스트-빌드-검증-모니터링을 자동화하는 체계로, 커밋 이후 빌드→테스트→배포→모니터링 순으로 진행된다. **MLOps**는 이 소프트웨어 파이프라인 개념을 머신러닝에 맞게 자동화한 것으로, 개발자·운영자·데이터 작업자 간 긴밀한 협업 문화와 방법론에 머신러닝 기술을 결합한 개념이다. 데이터 파이프라인(데이터 관리·전처리)과 머신러닝 파이프라인(훈련·평가, 기준 충족 시 엔드포인트 생성까지 자동화)을 하나로 연결해 구현한다. 파이프라인 자체 보안(리포지토리 커밋 시 코드 분석·검증 자동화, 빌드 이미지의 모듈/패키지 취약점 검사, 패키지 보안 테스트, 최근엔 AI를 활용한 버전별 테스트도 증가)과 파이프라인 내부 보안을 구분해 체계적으로 구현해야 한다.

CI/CD 구현 예시: 모델 훈련·전처리용 코드와 데이터는 각각의 리포지토리에서 관리되며, 훈련 코드가 업데이트/커밋되면 데이터 검증-전처리-모델 학습-모델 검증을 포함한 파이프라인이 자동 실행된다. 최종 생성된 모델 파일은 애플리케이션 파이프라인의 업데이트를 트리거하고, 애플리케이션 코드·모델 파일 변경이 CI/CD 파이프라인을 자동 실행시켜 배포-검증-승인까지 이어진다. 전처리 코드 변경 시에는 별도의 파이프라인 검증용 테스트 코드로 문제 여부를 확인한다. **CodeSuite**(Jenkins와 유사한 빌드 서비스, buildspec YAML로 테스트 스크립트 설정)로 빌드를 자동화하고, SageMaker Inference Recommender에 대한 로드 테스트(오버시스 기반 캐시 패키지로 부하 테스트 효율화)를 수행한 뒤 세그먼트를 생성해 A/B 테스트를 진행하기도 한다.

**SageMaker Pipelines**는 데이터 처리·모델 훈련·파인튜닝·평가·모델 등록·배포 과정을 자동화하며, GUI 또는 SDK로 생성할 수 있고 둘 다 최종적으로는 JSON 스키마 형태로 정의된다. JSON 스키마는 조건부 분기(예: F1 스코어 등 성능 지표가 목표치 도달 여부 판단)를 지원하며, 모델 등록·튜닝·EMR·오토ML·엔드포인트 관련 작업까지 통합 구성할 수 있다. 코드로 등록한 파이프라인은 SageMaker Studio GUI에서 프로세싱·트레이닝·조건 설정·모델 등록 과정을 실시간으로 확인할 수 있다. **SageMaker 파이프라인**이 모델 훈련·빌드에 집중한다면, **SageMaker 프로젝트**는 이 파이프라인과 CI/CD 파이프라인을 통합해 전체 MLOps 구조를 구현한다.

전체 자동화 흐름의 예: **CodeCommit**(코드 버전 관리) → **CodeBuild**(프로세싱 코드 포함 빌드, SageMaker 파이프라인을 가동하는 API 호출 코드 실행) → 파이프라인이 모델 훈련을 자동 진행 → 모델이 레지스트리에 등록되며 상태가 변경되면 **EventBridge**가 이를 감지 → 설정된 규칙에 따라 코드 파이프라인을 자동으로 트리거해 이후 배포 단계로 이어진다.

---

## 기타

### 운영 안내
- ADOMS 교육 플랫폼: 처음 접속 시 회원 유형 "기타" 선택 후 가입, 이메일 인증 필요(안 오면 리셋 버튼 또는 스팸함 확인). 기존 AWS 교육 수강자는 재가입 불필요.
- 교재는 VitalSource·SunParty 제휴 전자책으로 제공되며 PDF 다운로드·출력 가능, 태블릿(아이패드/갤럭시탭) 전용 클라이언트도 지원.
- 실습·교재는 스킬빌더(AWS) 사이트 → 마이 대시보드 → Enrolled Classes에서 "Machine Learning Engineering on AWS" 확인 후 접근(AWS Builder ID로 로그인).
- 설문조사는 처음 15명 → 이후 18명이 응답했으며, 목적은 콘텐츠 수준 조정과 AWS 경험 유무별 항목 구분 두 가지였다. 응답 결과 참가자 배경지식 수준이 다양함을 확인해 CS(컴퓨터 과학) 용어에 대한 질의응답도 함께 진행했다. 스킬빌더 교육 과정은 UI가 다소 구체적이지만 콘텐츠 구성이 우수해 향후 활용에 적합하다는 평가를 받았다.
- 와이파이 정보와 과정 상세 안내는 bit.ly 단축 링크로, 추가 자료·워크숍 접속 주소는 노션 페이지로 공유됨.
- 점심시간은 11:30~13:00이며, 교재 수령과 실습 사이트 접속 확인은 11시 50분까지 마쳐야 했음. 센터필드 지하 식당가는 혼잡하니 미리 식당을 정해두는 것을 권장. 그 밖에 오전 10시 24분~10시 35분, 오후 2시 5분까지, 오후 4시 40분까지 등 수시로 휴식·자유 실습 시간이 주어졌고, 오리엔테이션 관련 추가 설명은 35분에 진행될 예정이었음.
- 교육 수료 요건은 전체 과정의 3분의 2 이상 출석이며, 충족 시 취업에 활용 가능한 수료증이 발급됨. 성실 수강자 대상으로 마지막 날 스킬빌더 트리비아 플랫폼(익명 참여) 퀴즈를 통해 상품을 증정하며, 1등 외 추가 수상자 선정 방식도 검토 중이었음.
- 결석/출석 확인: 곽용진 님 결석, 김태균·김민석 님 등 출석 확인 진행.

### 여담
- 강사(김현규)는 본인 MBTI가 I(내향형)라 텐션을 높여 강의를 진행한다고 밝혔고, 수강생들의 MBTI도 확인하며 참여를 독려함.
- 강사가 "할머니가 읽어주는 듯한 목소리로 Windows 11 활성화 인증 보고서를 낭독"해달라고 AI에 요청한 일화 공유(과거 마이크로소프트에서 윈도우 정품 인증 보고서를 작성하던 추억에서 착안, 실제로 수면에 도움이 된다고 언급).
- 실습 사이 트래커밴드 노래를 함께 부르는 무대 준비/연습이 여러 차례 진행됨(가창 연습, 마지막 곡 준비 등).
- 발화자가 김세균 님을 찾으며 인공지능 라운지 팀 구성을 확인: 팀장·모델 감독·인프라 팀원 등 역할 구성(바지 주머니 위치에 비유해 설명), 백엔드 프로젝트에 쓸 모델을 개인정보 보호를 위해 API 없이 기기 내부(온디바이스)에서 구동하도록 최적화하는 방안과 메모리 사용량 최소화 기술을 멘토(박민재 님)와 논의. GPU 최적화 기술 동향과, IC 학회 전체 2위·노벨상 관련 발표 이력이 있고 ICNL 등 주요 분야의 권위자로 알려진 AI 분야 인사가 소개됨(로드컵·MSI에 비유해 해당 분야에서의 위상을 설명).
- 프로덕션 배포 논의 중 87번 시스템의 운영 현황이 언급됨. 실습 관련해서는 엑시모스(XGBoost 계열) 로봇 시스템 배포를 시도했으나 결과가 좋지 않았다는 공유도 있었음.
AI Infra 교육- 4일차

AI Infra 교육 4일차 과정 안내
1983년부터 시작된 새로운 교육 과정의 교재와 실습 집중 가능 여부를 확인해야 합니다.
교육 안내 사항이 포함된 특정 웹 페이지에 접속하여 세부 내용을 확인해야 합니다.

AI Infra 교육 4일차 실습 안내
마이클 사이트 내 'Developing Generative Applications on AWS' 과정에 접속하여 교재와 실습 내용을 확인해야 합니다.
지난 월요일에 진행했던 방식과 동일하게 실습을 수행하면 됩니다.

AI Infra 교육 과정 안내 및 확인
엠로드 클래스에서 교육 과정이 확인되지 않을 경우 운영팀에 전달할 수 있도록 공유를 요청함.
교육 과정 진행 방식에 대한 설명을 5분간 진행할 예정임.
참가자는 교재와 실습 환경을 확인하며 설명을 청취함.

AI Infra 교육 과정 개요
일주일간 진행되는 교육은 기존에 학습한 LLM 및 에이전틱 관련 지식을 기반으로 합니다.
AWS 환경에서 해당 기술들을 어떻게 구현하는지에 초점을 맞추어 실습이 진행됩니다.

AI Infra 교육 4일차 주요 학습 내용
LLM의 개념과 콘텐츠 생성 원리에 대해 학습하였습니다.
AWS 환경에서 RAG를 구현하는 방법과 LLM 및 RAG의 성능 평가 방안을 다루었습니다.
LLM 운영을 위한 가드레일 설정 방법에 대해 학습하였습니다.

Responsible AI 구현 및 에이전틱 AI 구조
Responsible AI는 유해 콘텐츠 생성 및 민감 데이터 유출을 방지하는 핵심 기능을 수행합니다.
에이전틱 AI 구현을 위해 가드레일 엔지니어링, 전체 구조 설계, 오케스트레이션, 메모리 관리 및 입력 데이터 처리 과정이 필요합니다.

AI 에이전트 및 가드레일 구성 교육
출력 제어와 가드레일 기능을 포함한 가이던스 엔지니어링의 전반적인 개념을 설명하였습니다.
에이전틱 AI 구현을 위한 비관리형 및 관리형 옵션을 비교하고 관리형 에이전트 코어의 활용 방안을 다루었습니다.

AI Infra 교육 4일차 실습 점검
교육 과정 중 다루었던 LangChain 및 SWOT SDK 프레임워크 활용 경험을 확인하였습니다.
교재 수령이나 실습 진행 과정에서 발생하는 기술적 이슈 여부를 점검하였습니다.

AI Infra 교육 4일차 운영 안내
점심시간은 오전 11시 30분부터 오후 1시까지로 운영할 예정입니다.
강의는 45분에서 50분 진행 후 10분에서 15분간 휴식하는 방식으로 운영됩니다.
머신러닝 과정의 향후 스케줄에 대한 논의가 진행될 예정입니다.

AI Infra 교육 4일차 운영 계획
기존 3일 과정에서 SageMaker 활용도를 높이기 위해 내용을 압축하고 워크숍 시간을 확보했던 방식과 유사하게 이번 교육을 운영할 계획입니다.
교육 스케줄의 모호한 부분을 정리하고 효율적인 진행 방식을 검토하고 있습니다.

AI Infra 교육 4일차 과정 구성 안내
Day 1과 Day 2의 교육 내용은 이미 수강생들이 숙지하고 있는 기초적인 내용으로 구성되어 있습니다.
모듈 1과 모듈 2의 배드(Bad) 및 프롬프트 엔지니어링 관련 내용은 짧게 요약하여 설명할 예정입니다.

AI Infra 교육 4일차 실습 계획
생성형 AI 애플리케이션을 웹 아키텍처에 통합하는 방법은 기존 지식을 바탕으로 간략하게 설명할 예정입니다.
실습 1번부터 3번까지의 내용은 수강생들의 숙련도를 고려하여 압축적으로 진행하고 교육 시간을 효율적으로 조정합니다.
금일 교육의 우선적인 목표는 실습 4번의 작업 1번과 2번을 완료하는 것입니다.

AI Infra 교육 4일차 및 향후 일정 안내
교육 과정의 실습을 포함한 전체 메뉴를 진행할 예정입니다.
내일 진행될 모듈 7번에서는 LLM 및 RAG 성능 평가를 다룹니다.
모듈 8번에서는 가드레일 관련 내용을 학습할 예정입니다.
이후 과정에서 모듈 9번과 10번을 중심으로 AI 관련 심화 내용을 다룰 계획입니다.

AI 에이전트 코어 실습 및 캡스톤 변경 안내
에이전틱 시스템 구현을 위한 에이전트 코어 활용 방안을 중점적으로 다룰 예정입니다.
기존 실습 5번의 캡스톤 과제를 에이전트 코어 실습으로 대체하여 진행합니다.
AWS 관리형 서비스 대신 에이전트 코어를 직접 활용하고 변경해보는 실습을 통해 학습 효과를 높이고자 합니다.

AI Infra 교육 4일차 모듈 1 진행 안내
AI Infra 교육 4일차 과정은 이틀에 걸쳐 진행됩니다.
모듈 1은 정리 문서를 통해 먼저 설명한 뒤 교재를 활용하여 내용을 복습하는 방식으로 진행됩니다.

생성형 AI의 개념 및 사용 사례
기존 프로그램 방식과 생성형 AI의 차이점을 설명하였습니다.
생성형 AI의 정의와 최신 트렌드를 바탕으로 주요 사용 사례를 논의하였습니다.

생성형 AI 음악의 빌보드 차트 진입 이슈
생성형 AI가 제작한 음악이 빌보드 차트에 진입하면서 관련 기술에 대한 관심이 높아졌습니다.
AI 음악의 차트 진입을 두고 음악적 소울의 부재를 이유로 비판적인 의견이 제기되는 등 논란이 발생했습니다.

생성형 AI의 입시 도입 현황
서울예술대학교가 입시 전형에 생성형 AI를 활용하겠다는 계획을 발표하였습니다.
생성형 AI 기술이 교육 제도권 내로 본격적으로 도입되는 추세가 확인되고 있습니다.

생성형 AI를 활용한 파워포인트 제작 효율화
생성형 AI의 업무 활용 중요성이 증대됨에 따라 파워포인트 제작에 이를 적극적으로 활용하고 있습니다.
마크다운 문서를 작성한 뒤 파워포인트 MCP를 호출하는 방식을 통해 높은 품질의 결과물을 얻을 수 있습니다.

생성형 AI 및 ML을 활용한 업무 효율화
생성형 AI와 ML 기술은 기업의 업무 수행 효율성을 높이는 방향으로 발전하고 있습니다.
Claude Code와 같은 도구들이 실제 업무 현장에서 활발하게 사용되고 있습니다.
금융 분야의 마스터카드와 같은 기업들에서도 이러한 기술을 적극적으로 도입하고 있습니다.

생성형 AI의 과외 교사 역할 확대
OpenAI는 질문 제시와 문제 풀이 힌트 제공이 가능한 과외 모드 기능을 개발하였습니다.
생성형 AI 기술의 발전으로 인해 기존 과외 교사의 입지가 점차 축소되고 있습니다.

AI 엔지니어의 학습 방향성 및 기본 개념
AI 엔지니어와 개발자를 목표로 하는 만큼 기술의 단순 사용을 넘어 동작 원리를 깊이 이해해야 합니다.
머신러닝과 생성형 AI를 포함한 모든 분야에서 파운데이션 모델과 같은 기본 개념을 숙지하는 것이 중요합니다.

파운데이션 모델의 학습 데이터 고갈 문제
파운데이션 모델 학습에 필요한 데이터가 인터넷상에서 수집 가능한 한계에 도달하고 있습니다.
일론 머스크는 향후 2년 내에 학습 가능한 데이터가 고갈될 것으로 전망했습니다.
데이터 고갈에 대한 대안으로 AI가 생성한 데이터를 학습에 활용하는 방안이 제시되었습니다.
AI 생성 데이터를 학습에 사용할 경우 발생할 수 있는 잠재적 문제점에 대한 논의가 필요합니다.

AI 모델의 콘텐츠 편향 및 생성 문제
학습 데이터에 내재된 편향이 모델의 콘텐츠 생성 과정에서 그대로 반영됩니다.
편향된 데이터를 반복적으로 학습함에 따라 모델의 편향성이 증폭되는 현상이 발생합니다.
이로 인해 창의적인 결과물보다는 틀에 박힌 정형화된 콘텐츠가 생성되는 문제가 나타납니다.

에이전트 AI의 자율성 레벨 소개
에이전트 AI의 자율성 레벨에 관한 개념을 설명함.
생성형 AI 및 에이전트 AI의 자율성 단계에 대한 자료를 공유함.

AI 모델 발전 단계 및 현주소
구글 딥마인드에서 제시한 AI 발전 5단계 모델을 기준으로 현재 기술 수준을 논의함.
분야별 차이는 존재하나 전반적으로 현재 AI 기술은 3단계에 도달해 있다고 평가함.

에이전트 AI 시스템 구축 및 운영 방식
단일 또는 멀티 에이전트 시스템은 각 에이전트에 LLM을 결합하여 구성합니다.
클라이언트의 요청을 처리하기 위해 추론, 행동, 관찰의 과정을 거치며 문제를 해결합니다.

인공지능의 발전 단계와 AGI의 정의
현재의 인공지능은 이전 세션의 대화 내용을 기억하고 반영하며, 요청 처리를 위해 다양한 도구를 활용하는 3단계 수준에 머물러 있습니다.
인간에 가까운 지능을 갖춘 AGI를 구현하기 위해서는 인공지능 기술이 4단계 및 5단계까지 발전해야 합니다.

AI 기술 발전 단계와 영화 매트릭스
AI 기술의 4단계는 AI가 스스로 학습하는 자율 학습 시대를 의미합니다.
5단계에 도달하면 AI가 고유한 성격까지 드러내는 수준으로 발전합니다.
영화 매트릭스에 등장하는 빨간 약과 파란 약의 개념을 통해 기술적 은유를 설명하였습니다.

영화 매트릭스를 통한 선택의 비유
영화 매트릭스에서 모피우스가 네오에게 현실을 직시할지 아니면 현재의 삶을 유지할지 선택하도록 제안하는 장면을 언급함.
빨간 약은 진실된 세상을 마주하는 선택을, 파란 약은 기존의 삶을 지속하는 선택을 상징함.

영화 매트릭스의 세계관과 핵심 주제
영화 매트릭스는 인공지능이 인간을 지배하는 가상 현실의 실체를 깨닫는 과정을 다룹니다.
인간이 인공지능의 생존을 위한 자원으로 이용되는 상황에서 인공지능에 맞서 싸우는 인간의 투쟁을 그립니다.

파운데이션 모델과 LLM의 개념 구분
AI 기술의 발전이 매트릭스와 같은 미래를 초래할 가능성에 대해 흥미로운 관점을 제시함.
파운데이션 모델은 LLM과 동일한 개념이 아니라는 점을 명확히 구분하여 설명함.

LLM의 콘텐츠 생성 원리
LLM이 생성하는 콘텐츠는 머신러닝 분류 모델과 같이 확률에 기반하여 작동합니다.
LLM은 다음에 올 토큰을 지속적으로 연산하고 예측하는 과정을 반복하여 문장을 완성합니다.

어텐션 메커니즘과 AI의 환각 현상
어텐션 메커니즘은 다음 토큰을 예측하는 과정에서 입력 데이터의 특정 위치에 집중하여 확률을 계산합니다.
AI가 생성하는 정보가 확률에 기반하기 때문에 사실과 다른 내용을 출력하는 환각 현상이 발생할 수 있습니다.

LLM의 정의 및 분류
LLM은 자연어를 이해하고 처리하며 콘텐츠를 생성할 수 있는 파운데이션 모델의 일종입니다.
모델 학습에 사용된 파라미터 규모가 매우 큰 경우를 Large Language Model이라 부르며, 상대적으로 작은 규모는 Small Language Model로 분류합니다.

Small Language Model의 활용 및 컨텍스트 윈도우
Small Language Model은 건축이나 의료와 같은 특정 산업 및 도메인 데이터에 특화되어 사용됩니다.
LLM을 활용하는 과정에서 Product Code나 Cursor 사용 경험이 있다면 컨텍스트 윈도우라는 개념을 접하게 됩니다.

컨텍스트 윈도우의 기술적 정의 및 구성 요소
컨텍스트 윈도우는 LLM 호출 시 필요한 다양한 데이터가 저장되는 기술적 공간을 의미합니다.
해당 공간에는 시스템 프롬프트, 사용자 프롬프트, 메모리 데이터, RAG 데이터, 이전 대화 내용 및 에이전트 호출 데이터 등이 포함됩니다.

LLM 컨텍스트 윈도우의 역할과 데이터 처리
LLM은 호출 시 필요한 모든 데이터를 컨텍스트 윈도우에 저장하여 활용합니다.
컨텍스트 윈도우는 단순한 기억 장치가 아니라 LLM 호출을 위한 데이터 저장 및 처리 공간으로 기능합니다.

모델별 컨텍스트 윈도우 사이즈 확인 방법
모델마다 컨텍스트 윈도우 사이즈가 다르므로 각 모델의 공식 문서를 통해 이를 확인해야 합니다.
최근 주목받는 모델인 페이퍼링(Papering)의 경우 컨텍스트 윈도우가 1메가 토큰을 지원합니다.

컨텍스트 윈도우와 데이터 압축의 이해
550K 워드나 1M 토큰의 용량이 충분해 보일 수 있으나, 실제 제품 코드나 대화형 에이전트를 활용하다 보면 컨텍스트 윈도우의 한계에 도달하게 됩니다.
컨텍스트 윈도우 내 데이터가 가득 차면 Product Agent가 기존 대화 내용을 자동으로 압축하는 과정을 경험하게 됩니다.

LLM 컨텍스트 윈도우 관리 및 추론 파라미터 활용
기존의 오래된 컨텍스트를 압축하여 제한된 컨텍스트 윈도우 사이즈를 효율적으로 확보합니다.
개발자는 입력 및 출력 데이터가 컨텍스트 윈도우를 통해 전달되는 과정을 이해해야 합니다.
LLM이 콘텐츠를 생성할 때 추론 파라미터와 토큰 사이즈 등을 활용하여 다음 턴을 준비합니다.

LLM 컨텍스트 윈도우 관리의 중요성
LLM마다 고유한 컨텍스트 윈도우 사이즈가 존재하며, 여기에는 과거 이력, 메모리, 도구, 프롬프트 등 다양한 데이터가 포함됩니다.
컨텍스트 윈도우 내 데이터의 효율적인 관리가 필수적으로 요구됩니다.

컨텍스트 윈도우 확장과 LLM 성능의 상관관계
컨텍스트 윈도우가 커지면 LLM에 더 많은 데이터를 전달할 수 있어 답변의 일관성을 높이고 잘못된 콘텐츠 생성을 방지할 수 있습니다.
많은 데이터를 처리하는 과정에서 LLM의 요청 처리 시간이 길어지는 성능 저하 문제가 발생합니다.

LLM 대화 세션의 답변 지연 현상 원인
LLM 대화 세션이 지속될수록 연산량 증가로 인해 답변 속도가 느려지는 레이턴시 현상이 발생합니다.
연산 과정이 추가됨에 따라 운영 비용이 증가하며 보안 공격에 취약해질 위험이 있습니다.

LLM 데이터 공격 및 이미지 생성 모델 원리
LLM에 유입되는 방대한 데이터로 인해 Detection 공격과 같은 보안 위협이 발생할 수 있습니다.
이미지 생성 모델인 Foundation 모델은 이미지 투 이미지(Image-to-Image) 방식으로 작동합니다.
해당 모델의 훈련 과정은 잉크 한 방울을 물에 떨어뜨리는 확산 원리에 비유하여 설명할 수 있습니다.

이미지 생성 모델의 훈련 과정 이해
이미지 생성 모델의 훈련은 원본 이미지에 단계별로 노이즈를 추가하여 최종적으로 노이즈로 가득 찬 상태를 만드는 과정을 학습하는 것입니다.
훈련 과정에서 이미지의 밝은 영역에는 노이즈를 많이 추가하고 어두운 영역에는 적게 추가하는 방식으로 노이즈 분포를 조절합니다.

Diffusion 모델의 원리와 Stable Diffusion의 특징
이미지 생성 추론은 노이즈가 적용된 정도를 단계별로 판단하여 이미지를 복원하는 과정을 거칩니다.
Stable Diffusion 모델은 훈련 및 추론 과정에서 발생하는 높은 메모리 사용량을 줄이기 위해 압축 기술을 활용합니다.

AI Infra 교육 4일차: Stable Diffusion 및 S3 활용
Stable Diffusion 모델을 활용하여 목표 일정을 효율적으로 관리하는 방안을 설명함.
웹 애플리케이션의 3D 아키텍처에서 S3를 프런트엔드 역할을 수행하는 웹 포트로 활용할 수 있음을 안내함.
내일 진행될 워크숍에서 S3를 이용한 웹 애플리케이션 구축 실습이 예정되어 있음.

백엔드 람다 함수와 API 게이트웨이의 역할
API 게이트웨이는 프런트엔드와 백엔드 사이에서 프락시 역할을 수행합니다.
백엔드에 위치한 람다 함수는 클라이언트 요청을 처리하는 과정에서 LLM을 호출하여 콘텐츠를 생성합니다.

AI Infra 교육 4일차 실습 안내
LLM을 활용한 간단한 에이전트 구성 및 호출 방식을 설명함.
내일 진행될 워크숍에서 터미널 PC를 통해 개발 서버를 구축하고 에이전트 코어를 호출하는 실습을 수행할 예정임.

서버리스 아키텍처의 장점 및 활용
S3, API Gateway, Lambda와 같은 서버리스 서비스는 기반 인프라를 직접 관리할 필요가 없습니다.
AWS의 모니터링 기능을 통해 성능 지표와 호출 로그를 즉시 구현하고 확인할 수 있습니다.
해당 아키텍처는 Foundation 모델이 가진 한계점을 보완하기 위해 제시되었습니다.

LLM의 주요 한계점과 데이터 컷오프
LLM은 사전 훈련 시점 이후의 새로운 데이터를 알지 못하는 지식 단절의 한계가 존재합니다.
새로운 LLM 모델이 출시되더라도 환각(Hallucination) 현상은 완전히 해결되지 않으며 여전히 발생합니다.

LLM의 한계와 해결 방안
LLM은 방대한 일반 데이터를 학습하지만 전문 지식이 부족하고 기업 특화 데이터를 반영하기 어렵다는 한계가 있습니다.
이러한 문제를 해결하기 위해 프롬프트 엔지니어링, RAG(검색 증강 생성) 도입, AI 에이전트 활용 등의 방안을 시도하고 있습니다.

AI Infra 교육 4일차 모듈 1 정리
AI 에이전트 시스템 구축, LLM 파인튜닝 등 생성형 AI 커스터마이징 기법과 AWS 활용 방안을 학습하였습니다.
모듈 1의 주요 내용을 요약하고 쉬는 시간을 가질 예정입니다.

생성형 AI와 Foundation 모델의 이해
생성형 AI의 핵심 기반인 Foundation 모델은 데이터를 학습하여 새로운 콘텐츠를 생성하는 역할을 수행합니다.
다양한 형태의 데이터를 처리하고 생성하는 기술을 멀티모달이라고 정의합니다.
Foundation 모델은 대규모 데이터를 사전에 훈련하여 구축됩니다.

AI 모델의 토큰 관리와 언어별 토큰 효율성
자연어 처리가 가능한 모델에서 개발자 관점의 토큰 관리는 컨텍스트 윈도우 활용 측면에서 매우 중요합니다.
동일한 의미를 가진 문장일 때 영어와 한국어 중 어느 언어가 더 많은 토큰을 소비하는지에 대한 질문이 제시되었습니다.

LLM 서비스의 토크나이저 확인 및 컨텍스트 관리
Claude와 같은 LLM 서비스는 사용자가 토크나이저를 확인할 수 있는 기능을 제공합니다.
AWS 또한 자체적인 토크나이저 도구를 제공하고 있습니다.
엔지니어 및 개발자에게는 컨텍스트 윈도우를 효율적으로 관리하는 것이 매우 중요합니다.

LLM 프롬프트 구성 및 모델 설정 교육
LLM 활용 시 사용자 프롬프트와 시스템 프롬프트를 효과적으로 구성하는 방법이 중요하게 다루어짐.
토큰 카운터 모델을 선택하고 엔트로피를 설정하는 등 모델 구성 및 적용 과정을 실습함.

텍스트 형식에 따른 토큰 수 변화
텍스트의 내용뿐만 아니라 JSON, JSONL, XML 등 데이터 포맷에 따라서도 토큰 수가 달라집니다.
토큰 수 변화를 확인하기 위해 인터넷상의 데모 사이트를 활용할 수 있습니다.

생성형 AI 개발 시 포맷별 토큰 소비량 고려
JSON, Pretty JSON, YAML 등 데이터 포맷에 따라 소비되는 토큰 양이 달라집니다.
생성형 AI를 개발할 때 비용 효율성을 위해 각 포맷별 토큰 소비 특성을 반드시 고려해야 합니다.

컨텍스트 윈도우의 개념과 한계
컨텍스트 윈도우는 단일 문장이라도 형태에 따라 다르게 구성될 수 있습니다.
컨텍스트를 활용하면 대화를 지속적으로 이어나갈 수 있습니다.
컨텍스트의 핵심적인 한계는 데이터가 임시적으로 저장된다는 점입니다.

컨텍스트 윈도우 제한에 따른 AI 성능 저하
대화가 지속되어 컨텍스트 윈도우의 토큰 사용량이 50%를 초과하면 AI의 응답 성능이 지연됩니다.
컨텍스트 윈도우가 가득 차면 AI가 이전 대화 내용을 망각하여 일관된 응답을 제공하지 못하는 문제가 발생합니다.

AI 모델 서비스 활용 및 메모리 개념
메모리 기술을 활용하여 Foundation 모델을 효율적으로 운용하는 방법을 설명함.
특정 사례에 최적화된 모델을 서비스 형태로 제공하여 개발 시간과 리소스를 절감할 수 있음.

AI 서비스 구성 및 플랫폼의 역할
AI 서비스는 인프라, 플랫폼, 서비스의 세 가지 계층으로 구성됩니다.
플랫폼은 기업의 AI 구현에 필요한 기능을 서비스 형태로 제공하는 역할을 수행합니다.
플랫폼의 활용 방식은 머신러닝 엔지니어링에서 다루는 비즈니스 문제 해결 과정과 동일합니다.

생성형 AI를 활용한 비즈니스 문제 해결 방안
고객 센터 직원의 높은 퇴사율 문제를 해결하기 위해 생성형 AI를 도입하여 업무 부담을 경감할 수 있습니다.
자주 묻는 질문이나 반복적인 루틴 업무를 챗봇이 대체하도록 하여 고객 센터의 운영 효율을 높일 수 있습니다.

생성형 AI 도입을 위한 단계별 판단 기준
생성형 AI 도입 전, 문제 해결에 머신러닝만으로 충분한지 먼저 판단해야 합니다.
졸업 예측과 같은 단순 정보 기반의 분석 작업은 생성형 AI가 아닌 머신러닝 모델로 수행하는 것이 적합합니다.
머신러닝으로 해결할 수 없는 경우, 에이전틱 AI 애플리케이션 개발에 필요한 데이터를 확보하는 것이 2단계입니다.

LLM 선택 시 고려 사항
에이전트의 역할과 목적에 따라 하나 이상의 LLM을 조합하여 사용할 수 있습니다.
시중에 존재하는 수많은 모델 중 비용 효율성을 최우선으로 고려하여 적합한 LLM을 선택해야 합니다.

AI 인프라 교육 4일차: 모델 배포 및 평가
프롬프트 엔지니어링, RAG, 에이전트, 웹 에이전트 및 하네스 엔지니어링을 결합하여 구성한 후 배포하는 과정을 설명하였습니다.
생성형 AI 평가 모듈을 통해 적절한 LLM을 선택하고 평가하는 방법을 안내하였습니다.
파운데이션 모델의 개념과 활용 방안에 대해 교육을 진행하였습니다.

AI Infra 교육 4일차 휴식 및 실습 안내
교육 진행 중 10시 35분까지 휴식 시간을 갖기로 결정하였습니다.
휴식 이후 추수법에 대한 설명과 함께 학습 실습을 진행할 예정입니다.

Bedrock 서비스 및 LLM 호출 방법 교육
컨텍스트 엔지니어링과 토큰화 관련 내용을 추후 학습 자료로 참고할 것을 권장함.
Bedrock 서비스의 정의와 이를 활용한 LLM 호출 방법에 대해 교육을 진행함.

LLM API 호출 및 사용자 프롬프트의 이해
Upstage의 LLM API 호출 방식은 일반적인 API 호출 과정과 동일한 원리로 작동합니다.
사용자가 LLM에 전달하는 요청은 사용자 프롬프트라고 지칭합니다.
사용자 프롬프트는 LLM에 전달되는 전체 데이터 중 일부에 해당합니다.

LLM 입력 데이터 구성 요소
LLM의 입력 데이터는 사용자 프롬프트와 이전 턴에서 LLM이 생성한 대화 내용을 포함합니다.
LLM의 응답 톤과 매너를 결정하는 시스템 프롬프트가 입력 데이터에 포함됩니다.
외부 도구를 통해 검색된 콘텐츠와 RAG(레거)와 같은 데이터베이스의 정보가 입력 데이터로 활용됩니다.

LLM 추론 파라미터 및 컨텍스트 활용
LLM은 확률 기반으로 콘텐츠를 생성하므로 추론 파라미터를 통해 생성 확률을 조절할 수 있습니다.
지정된 콘텐츠와 파라미터는 LLM의 컨텍스트 윈도우 내에서 컨텍스트 형태로 전달됩니다.

LLM 추론 파라미터의 종류와 활용
사용자 프롬프트는 LLM 추론 과정의 일부 요소에 해당합니다.
추론 파라미터에는 템플릿, 카피, 4K, 컨시퀀스 등이 포함됩니다.
템플릿은 대부분의 LLM에서 공통적으로 사용되나, 카피나 4K 등은 모델별로 지원 여부가 다를 수 있습니다.

LLM 호출 파라미터 설정: 템플릿과 Top-P
템플릿은 LLM이 생성하는 콘텐츠의 자유도와 일반성을 0에서 1 사이의 값으로 설정하는 파라미터입니다.
Top-P는 LLM이 생성할 후보군을 확률 분포에 따라 몇 퍼센트까지 포함할지 결정하는 0에서 1 사이의 설정값입니다.

LLM 토큰 생성 확률 및 후보군 선정
낮은 확률 값 설정 시 확률이 높은 단어 위주로 후보가 선정됩니다.
높은 확률 값 설정 시 상대적으로 낮은 확률의 토큰까지 후보군에 포함됩니다.
예시를 통해 '마법의(magical)' 다음 토큰으로 '숲(forest)'이 0.35, '기타(guitar)'가 0.25의 확률로 계산되는 과정을 설명합니다.

의존 확률 기반의 단어 후보군 선정 방식
의존 확률 임계값에 따라 후보군에 포함되는 단어의 범위가 결정됩니다.
임계값이 0.4일 때는 확률이 높은 단어만 후보군에 포함되지만, 0.8로 높아지면 더 넓은 범위의 단어들이 후보군으로 선정됩니다.
선정된 후보군들은 템플릿과 조합되어 최종 단어를 도출하는 과정을 거칩니다.

AI 모델 생성 제어 파라미터 설명
상위 단어 선정 개수는 카피 수에 따라 상위 3개 또는 5개 등으로 설정됩니다.
Stop Sequence는 특정 시퀀스를 지정하여 모델의 콘텐츠 생성을 중단시키는 역할을 합니다.
Max Tokens는 단일 응답에서 생성 가능한 최대 토큰 수를 제한하여 응답 길이를 조절합니다.

LLM 응답 제어 및 컨텍스트 관리 기법
LLM의 응답 길이를 제한하여 컨텍스트 윈도우 사용량을 효율적으로 관리할 수 있습니다.
응답 크기를 줄이면 다음 턴의 컨텍스트로 유입되는 데이터 양이 감소하여 컨텍스트 사이즈를 절약할 수 있습니다.
반복적인 단어나 문구 생성을 방지하기 위해 리피티션 페널티(Repetition Penalty) 기법을 활용합니다.

Amazon Bedrock의 API 호출 및 추론 방식
Amazon Bedrock은 관리 콘솔, CLI, SDK 등 다양한 인터페이스를 통해 API 호출 및 응답 형식으로 추론을 요청하는 방식으로 사용합니다.
대화나 이메일 작성 시 동일한 의미를 가진 단어를 일관되게 사용하는 패러프레이징 등의 추론 파라미터 설정이 중요합니다.

AWS Bedrock 서비스 엔드포인트의 특징 및 활용
AWS 서비스 엔드포인트는 대부분 서비스별 및 리전별로 고유한 주소를 가집니다.
SDK, CLI, 관리 콘솔 사용 시에는 엔드포인트 주소를 직접 알 필요가 없습니다.
일반 애플리케이션 개발 시에는 코드 내에 해당 엔드포인트 주소를 명시해야 합니다.

Amazon Bedrock 파운데이션 모델 권한 설정
Amazon Bedrock 서비스에서 사용할 파운데이션 모델을 선택하고 권한을 부여하는 설정을 진행합니다.
Claude나 Amazon Nova와 같은 특정 모델을 사용할 수 있도록 접근 권한을 구성하는 방법을 설명합니다.

Amazon Bedrock의 모델 카탈로그 확인 방법
Amazon Bedrock 콘솔 내 Configuration 메뉴의 Model Access 항목에서 사용 가능한 모델 목록을 확인할 수 있습니다.
Bedrock은 기본적으로 LLM 카탈로그 기능을 제공하여 사용자가 모델 목록을 쉽게 조회하고 관리할 수 있도록 지원합니다.

LLM 솔루션 업체 및 API 활용 방법
Amazon, Anthropic, Google, Meta, NVIDIA, OpenAI 등 다양한 업체의 LLM을 API 호출을 통해 서비스처럼 이용할 수 있습니다.
Bedrock 환경에서 LLM을 호출하고 응답을 처리하는 방식의 실습을 진행합니다.

Bedrock 파운데이션 모델 호출을 위한 권한 설정
백엔드 함수가 파운데이션 모델의 추론을 요청하기 위해서는 해당 람다 함수에 적절한 권한 설정이 필요합니다.
Bedrock에서 제공하는 각 모델의 모델 ID를 확인하고, 해당 모델에 대한 호출 권한을 허용해야 합니다.

Bedrock SDK 설정 및 권한 관리
AWS Bedrock 사용 시 Deny와 Allow 정책을 활용하여 적절한 권한 설정을 수행해야 합니다.
개발자 및 엔지니어는 Bedrock SDK를 활용하며, 이를 위해 필요한 라이브러리를 설치하고 AWS 환경을 설정해야 합니다.

AWS 인증 설정 및 Bedrock 구조
AWS 인증을 위해 액세스 키 ID, 시크릿 액세스 키, 리전 정보를 SDK 터미널에 환경 변수로 설정해야 합니다.
Amazon Bedrock은 크게 컨트롤 플레인과 데이터 플레인으로 구분됩니다.

Bedrock 관리 기능 및 리소스 태그 활용
Bedrock은 LLM 사용 시 필요한 관리 기능을 API 형태로 제공합니다.
특정 리전에서 사용 가능한 LLM 조회, 배치 작업 수행 등의 기능을 지원합니다.
리소스에 키와 값 기반의 태그를 설정하여 리소스의 성격을 관리할 수 있습니다.

리소스 태그 관리 및 AI 인프라 구조
리소스의 성격, 서비스 연관성, 관리 주체 등을 식별하기 위해 태그를 설정합니다.
LLM의 동작을 제어하는 영역은 컨트롤 플레인에서 담당합니다.
LLM에 대한 호출 요청과 응답 처리는 데이터 플레인에서 수행합니다.

AI 인프라 API의 컨트롤 플레인과 데이터 플레인 구분
LLM 호출 방식은 동기식 호출, 스트리밍 응답, 표준화된 대화 인터페이스 호출 등으로 나뉩니다.
컨트롤 플레인 API는 모델 생성 및 관리에 사용되며, 데이터 플레인 API는 실제 모델 적용을 담당합니다.
컨트롤 플레인과 데이터 플레인은 서로 다른 엔드포인트 주소를 사용합니다.

Bedrock API 리전별 엔드포인트 구성
서울 리전의 Bedrock 컨트롤 플레인과 데이터 플레인인 런타임 API는 각각 고유한 주소 체계를 가집니다.
Bedrock 런타임은 데이터 플레인 기능을 수행하며 서울 리전에서는 Bedrock-runtime.ap-northeast-2 주소를 사용합니다.
에이전트 및 나리지 베이스 생성을 관리하는 API는 별도의 주소 체계로 운영됩니다.

Bedrock 에이전트 런타임 및 데이터 형식
Bedrock 에이전트는 각기 다른 런타임 환경을 사용하므로 이를 사전에 확인해야 합니다.
Bedrock 모델의 호출과 응답은 모두 JSON 형식을 기반으로 이루어집니다.
간단한 예제를 통해 JSON 기반의 모델 호출 및 응답 방식을 확인할 수 있습니다.

인보킹 모델 API 호출 실습
인보킹 모델 API를 호출하는 구체적인 방법을 시연함.
교재에 기재된 예시 코드를 활용하여 모델 설정 및 API 호출 과정을 설명함.

Bedrock LLM 모델 호출 및 설정 안내
사용자 프롬프트 내에서 모델 ID를 선택하고 최대 토큰 수와 온도(Temperature)와 같은 하이퍼파라미터를 설정할 수 있습니다.
Amazon Bedrock의 LLM은 요청과 응답이 모두 JSON 형식으로 처리되는 네이티브 방식을 사용합니다.

Amazon Bedrock Invoke Model API 호출 방식
텍스트 형태의 네이티브 요청을 JSON 형식으로 변환하는 과정을 거칩니다.
Amazon Bedrock에서 정의한 LLM별 API 요청 형식에 맞춰 Invoke Model API를 호출합니다.
Claude 모델의 API 명세에 따라 요청 형식을 작성하여 호출을 수행합니다.

JSON 응답에서 텍스트 추출 방법 안내
공식 문서의 구조를 따라 작성된 JSON 형식의 응답 데이터를 디코딩해야 합니다.
디코딩된 데이터에서 필요한 텍스트 정보만을 추출하는 구체적인 방법에 대해 질문을 던졌습니다.

JSON 데이터 추출 및 경로 탐색 안내
인터넷에서 제공되는 JSON Path Finder 도구를 활용하여 구조가 없는 JSON 데이터에서 특정 정보를 추출하는 방법을 설명함.
HTTP 헤더 내 커넥션 정보와 같이 JSON 데이터 내 특정 경로에 위치한 값을 규칙에 따라 탐색하는 방식을 안내함.

Invoking Model API 호출 및 텍스트 추출 방법
JSON 응답 데이터에서 경로를 지정하여 특정 텍스트 항목만을 추출하는 방법을 설명함.
콘텐츠 배열 내의 텍스트를 추출한 뒤 출력하는 과정을 통해 순수 텍스트 데이터를 확보할 수 있음.
Invoking Model API 호출 시 데이터 추출 과정을 거치지 않으면 불필요한 정보가 포함되므로 반드시 추출 절차를 수행해야 함.

LLM API 응답 데이터의 구성 및 활용
API 응답에는 Request ID, Response Code, 호출 시각, 콘텐츠 타입, 토큰 사용량 등 상세한 메타데이터가 포함됩니다.
해당 정보는 서비스 운영보다는 주로 개발 과정에서의 디버깅 목적으로 활용됩니다.
실제 사용자 대상 서비스 제공 시에는 응답 데이터에서 텍스트 본문만을 추출하여 사용해야 합니다.

인보킹 모델과 스트리밍 방식의 차이
인보킹 모델 API는 모든 토큰의 확률 계산을 완료하여 문장이 완성된 후 한꺼번에 응답을 제공합니다.
인보킹 모델 위드 리스폰스 스트림은 토큰이 생성되는 즉시 클라이언트에게 전달하는 스트리밍 방식을 사용합니다.

스트리밍 방식의 데이터 출력 및 토큰 처리
스트리밍 형식으로 생성되는 콘텐츠는 반복적인 추출 과정을 통해 답변을 구성합니다.
네이티브 JSON 데이터 내의 콘텐츠는 스트리밍으로 출력되는 개별 토큰들을 의미합니다.

콘텐츠 블록 델타를 활용한 스트리밍 응답 구현
콘텐츠 블록 델타 유형은 실제 답변 콘텐츠가 생성되는 핵심 영역입니다.
반복문과 콘텐츠 블록 데이터를 추출하여 스트리밍 방식으로 응답을 제공하는 코드가 구현되어 있습니다.

Bedrock Invoke Model API 호출 및 설정
Invoke Model with Response Stream API를 사용하여 모델을 호출하는 방법을 설명함.
사용하는 LLM의 종류에 따라 호출 시 설정해야 하는 데이터 구조가 달라짐을 명시함.

LLM 통합 API 활용을 위한 커버스 API
다양한 LLM 모델을 사용할 때 모델별로 다른 구조를 표준화하여 API를 호출할 수 있는 커버스(Coverse) API 기능을 제공합니다.
커버스 API는 일반 호출 응답 방식과 스트리밍 응답을 지원하는 커버스 스트림 API로 구분됩니다.
모델 공급자와 관계없이 통합된 형식으로 API를 호출할 수 있어 개발 효율성을 높일 수 있습니다.

커버스(Bedrock) API의 추론 파라미터 설정 방식
일반적인 API와 달리 커버스(Bedrock) API는 추론 파라미터를 설정할 때 별도의 인퍼런스 컨피그(Inference Config) 구조를 사용합니다.
인보킹 모델 API는 추론 파라미터를 직접 지정하는 반면, 커버스 API는 인퍼런스 컨피그 내부에 파라미터를 정의해야 합니다.

커버스 API의 구조 및 멀티턴 호출 기능
커버스 API는 파운데이션 모델의 종류와 관계없이 일관된 구조로 파라미터를 설정하여 호출할 수 있습니다.
커버스 API는 챗봇과 같이 하나의 세션 내에서 대화를 주고받는 멀티턴 호출 기능을 지원합니다.

LLM 호출 방식과 턴(Turn) 개념
클라이언트가 LLM을 호출하고 응답을 받는 과정을 한 턴(Turn)이라고 정의합니다.
챗봇과 같이 대화가 지속되는 멀티턴 형식의 구현에는 커버스(Conversational) API를 사용해야 합니다.
인보킹(Invoking) API는 이미지 생성과 같이 단일 턴으로 작업이 종료되는 경우에 적합합니다.

Invoking Model API와 Converse API의 차이점
Converse API는 대화형 목적에 특화되어 있으며 문서 지원 및 일반적인 형식을 제공합니다.
Invoking Model API는 콘텐츠 생성이나 단발성 작업에 주로 사용되며 추론 파라미터를 직접 제어할 수 있습니다.

AI Infra 교육 4일차 모듈 및 실습 안내
이번 모듈에서는 모델 응답 생성 방식과 프로그래밍을 통한 Bedrock 활용법을 다룹니다.
코드 수정이 필요한 관계로 데모 시연은 잠시 뒤로 미루고 실습을 먼저 진행합니다.

LLM 호출 데이터 및 추론 파라미터 설명
LLM 호출 시 컨텍스트 윈도우에 포함되는 다양한 데이터의 구성 요소를 설명하였습니다.
토큰 수와 스탑 시퀀스는 토큰의 양을 제어하는 요소임을 명시하였습니다.
Temperature와 Top-P는 추론 과정에서 확률과 관련된 파라미터임을 설명하였습니다.

Bedrock LLM 활용 방식 및 서비스 엔드포인트
Bedrock의 LLM 기능은 SDK, CLI, 관리 콘솔 등 다양한 방식을 통해 개발 및 호출이 가능합니다.
Bedrock은 서비스 운영을 위해 데이터 플레인과 컨트롤 플레인으로 구분된 서비스 엔드포인트를 제공합니다.

Bedrock SDK를 활용한 파운데이션 모델 목록 조회
Bedrock SDK는 다양한 프로그래밍 도구를 지원하며, 실습에서는 주로 파이썬을 사용합니다.
Bedrock 클라이언트를 생성한 후 list_foundation_models API를 호출하여 파운데이션 모델 목록을 조회할 수 있습니다.
API 호출을 통해 모델 목록에 대한 응답을 확인하고 모델을 실행하는 과정을 수행합니다.

Bedrock Converse API의 특징 및 구조
Converse API는 사용하는 파운데이션 모델이나 LLM의 종류와 관계없이 통합된 호출 및 응답 구조를 제공합니다.
메시지 내 시스템 프롬프트와 인퍼런스 컨피그를 통해 추론 파라미터를 설정할 수 있습니다.
API를 통한 모든 응답은 JSON 형식으로 전달됩니다.

AI Infra 교육 4일차: 모듈 3 개요
코드 엔진에 대한 사전 이해를 바탕으로 모듈 3 과정을 빠르게 진행합니다.
LLM이 가진 지시 단절, 환각, 커스터마이즈 부족 및 특정 도메인 지식의 한계를 해결하기 위해 프롬프트 엔지니어링부터 학습을 시작합니다.

AI 인프라 교육 4일차: 프롬프트 작성법
이번 모듈은 AI 모델의 능력을 극대화하기 위한 효과적인 프롬프트 작성 방법을 학습하는 것을 목적으로 합니다.
실제 코드 작업 요청 시 프롬프트의 품질이 결과에 중요한 영향을 미치므로 기초부터 체계적으로 다룹니다.

코드 에이전트 활용을 위한 프롬프트 작성법
코드 에이전트가 정확한 결과물을 생성할 수 있도록 프롬프트를 한 문장으로 요약하지 말고 최대한 자세하게 구조화하여 작성해야 합니다.
프롬프트는 파운데이션 모델이 특정 작업을 수행하고 콘텐츠를 생성하도록 지시하는 가이드라인 역할을 합니다.

효과적인 프롬프트 작성을 위한 구성 요소
프롬프트는 태스크, 지침, 입력, 출력, 예시와 스타일, 페르소나, 제한 조건 등 7가지 핵심 요소를 포함해야 합니다.
단순히 한 문장으로 요청하기보다 구체적인 구성 요소를 모두 갖추어 작성할 때 원하는 결과물을 얻을 수 있습니다.

LLM 기반 제조 재고 관리 프로세스 개선 교육
LLM에 공급망 추적 및 분석 전문가라는 페르소나를 부여하여 역할을 설정합니다.
제조 회사의 재고 관리 프로세스를 검토하고 분석 및 개선 계획을 수립하는 것이 LLM의 주요 작업 임무입니다.
제공된 입력 데이터를 바탕으로 상장 분석과 구체적인 개선 방안을 도출합니다.

AI 인프라 교육 4일차 답변 양식 및 출력 지침
답변 작성 시 프로세스 비용, 문제점 분석, 개선 권장 사항, 구현 계획, 예산 결과 순으로 구성해야 합니다.
출력 형식은 마크다운을 사용하여 글머리 기호와 텍스트를 작성해야 합니다.

LLM 출력 형식 및 작성 가이드라인 설정
LLM의 출력 형식을 마크다운으로 정의하여 일관된 구조를 확보합니다.
보고 대상에 맞춰 전문적이고 정보에 기반한 어조와 스타일을 적용하도록 설정합니다.
문법에 어긋나는 표현이나 존중하지 않는 언어 사용을 금지하는 제약 조건을 명시합니다.

시스템 프롬프트의 정의와 역할
시스템 프롬프트는 에이전틱 AI나 생성형 AI 솔루션 개발자가 작성하는 프롬프트입니다.
AI의 행동 지침서로서 AI의 일반적인 행동 방식과 성격, 그리고 경계를 정의하는 역할을 수행합니다.

AI 솔루션 내 시스템 프롬프트의 역할과 유추
AI 솔루션 업체들은 서비스 제공 시 시스템 프롬프트를 사전에 설정하여 적용합니다.
사용자가 입력하는 프롬프트에는 사용자가 인지하지 못하는 시스템 프롬프트가 결합되어 전송됩니다.
시스템 프롬프트가 외부로 유추되는 사례가 발생하며 관련 뉴스 사례를 통해 이를 확인할 수 있습니다.

시스템 프롬프트의 개념 및 유출 사례
시스템 프롬프트는 AI가 대화를 시작하기 전 성격, 규칙, 도구 사용법 등을 정의하여 미리 부여하는 설정 파일입니다.
과거 GPT를 포함한 AI 제품들의 시스템 프롬프트가 외부로 유출되어 유튜브 등에 공개되는 사건이 발생했습니다.
현재 AI 제품들의 시스템 프롬프트를 모아둔 저장소에는 48,940개의 데이터가 등록되어 있습니다.

AI 모델의 프롬프트 지시서 추출 및 관리
AI 모델은 마법처럼 작동하는 것이 아니라 사전에 정의된 지시서에 따라 움직입니다.
모델에게 이전 지시 내용을 그대로 출력하도록 요청하는 방식으로 프롬프트 지시서를 추출할 수 있습니다.
사용자와 커뮤니티는 이러한 프롬프트 지시서들을 수집하여 한곳에 정리하고 공유하고 있습니다.

AI 솔루션의 시스템 프롬프트 관리 및 유출 원인
현재 11개 이상의 기업용 AI 솔루션 폴더가 관리되고 있으며, Claude 3.5 Sonnet과 같은 최신 모델 업데이트가 실시간으로 반영되고 있습니다.
시스템 프롬프트가 유출되는 주요 원인은 해당 프롬프트가 각 AI 솔루션 업체의 개발자에 의해 직접 작성되어 관리되기 때문입니다.

AI 솔루션 개발을 위한 1차 자료 활용법
AI 솔루션 개발 시 논문 등 1차 자료를 참고하여 지시문 작성법, 도구 호출 시점, 거절 기준 및 말투 등을 학습할 수 있습니다.
강의나 블로그 요약본 대신 워싱턴 포스트와 같은 1차 자료를 활용하면 실제 업무와 스타트업 개발 환경에서 유용한 인사이트를 얻을 수 있습니다.

시스템 프롬프트의 구성 요소 및 확인 방법
GitHub에서 유출된 시스템 프롬프트를 검색하여 AI 솔루션의 지침서를 확인할 수 있습니다.
시스템 프롬프트에는 작업 내용, 성격, 연락 정보, 스타일 등 AI의 운영 지침이 포함되어 있습니다.

프롬프트 엔지니어링의 핵심 기법
프롬프트 엔지니어링은 모델의 추적 능력을 활용하여 충분한 콘텐츠를 요청하는 것이 중요합니다.
요청 내용과 최대한 유사한 예제를 제공하는 퓨샷(Few-shot) 방식을 활용해야 합니다.
복잡한 요청을 논리적인 단계로 나누어 처리하는 체인 오브 쏘트(Chain-of-Thought) 기법을 적용해야 합니다.

AI 모델의 답변 최적화 기법
셀프 컨시스턴시는 모델이 여러 답변을 생성한 뒤 다수결을 통해 최종 답변을 도출하는 방식입니다.
트리 오브 소츠는 전략적 탐구가 필요할 때 여러 답변을 생성하고 비교하여 최적의 결과를 찾아가는 기법입니다.
트리 오브 소츠는 세 명의 전문가가 질문에 답변하는 것과 같이 다수의 경로를 통해 문제를 해결합니다.

에이전틱 AI의 논리적 답변 생성 및 ReAct 기법
LLM은 복잡한 질문에 대해 여러 갈래의 논리적 답변을 생성하고 이를 상호 교환하여 최종적인 답변을 도출합니다.
에이전틱 AI의 핵심 기법인 ReAct는 추론과 행동을 반복하는 과정을 통해 답변의 정확도를 높입니다.
지정된 단계에서 생각, 행동, 관찰의 반복을 거쳐 최종적인 결과물을 생성합니다.

프롬프트 템플릿의 개념 및 활용 예시
프롬프트 템플릿은 챗봇 애플리케이션 개발 시 특정 목적에 맞는 대화 구조를 설계하는 역할을 수행합니다.
고객 상담 센터 챗봇을 예로 들면, 사용자의 질문 분야를 먼저 확인하는 초기 응대 과정을 프롬프트 템플릿으로 구현할 수 있습니다.

AI 인프라 교육: 프롬프트 템플릿 선택 및 활용
사용자가 모바일, 가정, 반도체, SSD, HDD 등 특정 분야를 선택하면 해당 분야에 최적화된 프롬프트 템플릿이 활성화됩니다.
선택된 분야 정보가 프롬프트 템플릿에 자동으로 반영되어 LLM으로 전달됩니다.

프롬프트 템플릿 사용의 목적과 이점
프롬프트 템플릿을 사용하는 주된 목적은 사용자 질문을 체계화하고 구조화하는 데 있습니다.
구조화된 입력을 통해 챗봇이 엉뚱한 질문에 대응하는 오류를 방지하고 보다 정확한 답변을 생성할 수 있습니다.

프롬프트 템플릿의 활용 및 엔지니어링
사전에 정의된 프롬프트 템플릿을 활용하여 일반적인 응답을 생성할 수 있습니다.
구축된 프롬프트 템플릿은 타사의 고객 센터 챗봇 등 다양한 환경에서 재활용이 가능합니다.
프롬프트 엔지니어링 기술을 통해 사전에 정의된 템플릿을 효과적으로 구성할 수 있습니다.

AI 모델의 프롬프트 활용 및 제약 조건 설정
프롬프트 엔지니어링을 통해 답변의 품질과 관련성을 향상할 수 있습니다.
시스템 프롬프트를 활용하여 모델의 기본 규칙을 사전에 정의할 수 있습니다.
Stable Diffusion과 같은 이미지 생성 모델에서는 생성 결과에서 제외할 요소를 지정하는 네거티브 프롬프트를 사용합니다.

Stable Diffusion 네거티브 프롬프트 활용법
Stable Diffusion 모델을 이용한 이미지 생성 시, 원치 않는 결과물을 방지하기 위해 네거티브 프롬프트를 사용합니다.
네거티브 프롬프트에는 이미지의 품질 저하를 막기 위해 'ugly', 'watermark', 'beginner'와 같은 부정적인 요소들을 포함합니다.

역할 기반 프롬프트와 AI 에이전트의 연관성
역할 기반 프롬프트는 특정 전문가의 페르소나를 설정하여 AI가 해당 역할에 맞는 결정을 내리도록 유도합니다.
사용자 프롬프트 구조 내에서 스타일과 페르소나를 정의하는 이러한 기법은 향후 AI 에이전트 구현으로 이어집니다.

AI 에이전트 정의 및 역할 기반 프롬프트 작성
AI 에이전트를 정의하는 마크다운 문서에는 에이전트의 역할에 관한 구체적인 내용이 포함되어야 합니다.
에이전트의 역할을 자연어로 작성하는 과정은 역할 기반 프롬프트 설계의 일환으로 간주됩니다.

AI 에이전트 개발과 프롬프트 엔지니어링의 연관성
AI 에이전트 개발 과정에서 프롬프트 엔지니어링 학습이 필수적으로 요구되는 이유를 설명함.
모듈 3 교재 내용을 검토한 후 VPN 실습을 진행할 예정임.

효과적인 LLM 프롬프트 작성을 위한 구성 요소
LLM의 성능을 높이기 위해 역할 정의, 태스크, 지침, 입력값, 출력 형식, 제약 조건 등을 포함하여 프롬프트를 작성해야 합니다.
프롬프트 작성 시 구체적인 예시를 활용하여 원하는 출력 결과를 명확하게 유도하는 것이 중요합니다.

LLM 평가 및 추천 서비스 프롬프트 활용
LLM 평가 과정에서 프롬프트가 수행하는 역할을 설명함.
쇼핑몰 추천 서비스 구현 시 템플릿을 활용하여 사용자별 제품 추천 값을 동적으로 생성하는 방식을 언급함.

LLM 모델 평가를 위한 프롬프트 활용
동일한 시스템 프롬프트를 사용하여 모델별 응답을 비교함으로써 최적의 프롬프트와 모델을 선정할 수 있습니다.
프롬프트를 통한 인간 평가 방식은 LLM 선택을 위한 정통적인 평가 방법으로 활용됩니다.
해당 평가 방식에 대한 상세한 내용은 다음 날 교육에서 다룰 예정입니다.

프롬프트 엔지니어링 모범 사례 및 가이드
프롬프트 작성 시 컨텍스트를 충분히 포함하고 복잡한 텍스트는 분할하여 처리하는 것이 효과적입니다.
엔트로픽의 클로드 모델을 위한 공식 프롬프트 가이드가 제공되고 있습니다.

프롬프트 엔지니어링 베스트 프랙틱스 개요
모델 활용 시 시스템 프롬프트와 템플릿 작성에 도움이 되는 베스트 프랙틱스를 소개함.
프롬프트 작성 시 모호함을 배제하고 명확하며 직접적인 지시를 사용하는 것이 중요함.

프롬프트 엔지니어링 기법 및 구조화 교육
미국식 프롬프트 작성 스타일을 준수하고 컨텍스트 추가 및 예제 활용을 통해 프롬프트의 효과를 높여야 합니다.
엑셀 태그를 활용하여 프롬프트 구조를 설계하고 명확한 역할을 부여하는 것이 중요합니다.
주요 프롬프트 기법으로 예제를 활용하는 퓨샷(Few-shot)과 단계별 사고를 유도하는 체인 오브 소트(Chain-of-Thought)를 참고해야 합니다.

AI 인프라 교육 4일차: 프롬프트 최적화 기법
외부 데이터베이스 연동 및 에이전트 도구 활용을 통해 프롬프트 성능을 개선합니다.
ReAct 방식을 도입하여 모델이 도구를 사용하도록 설계합니다.
솔루션 업체의 모범 사례를 참고하고 프롬프트 템플릿을 적극적으로 활용합니다.
복잡한 작업을 단계별로 세분화하여 순차적으로 처리하는 방식을 적용합니다.

오전 강의 종료 및 점심시간 안내
오전 강의를 11시 25분에 마무리하고 점심시간을 갖습니다.
오후 1시부터 모듈 4번 강의를 이어서 진행할 예정입니다.

생성형 AI 애플리케이션의 멀티 리전 구축 전략
여러 리전을 활용하여 생성형 AI 애플리케이션을 구축할 때 인텔리전트 라우팅 리소스를 활용할 수 있습니다.
서울과 도쿄 등 각 리전의 앞단인 글로벌 인프라 영역에 인텔리전트 라우팅 리소스를 배치하여 LLM 서비스의 효율성을 높입니다.

인텔리전트 라우팅을 통한 LLM 추론 요청 처리
인텔리전트 라우팅 지점은 프론트엔드 및 Route 53 엔드포인트와 유사한 역할을 수행하며 클라이언트의 LLM 추론 요청을 최초로 수신합니다.
수신된 요청은 우선 서울 리전으로 전달되어 콘텐츠 생성을 수행합니다.
서울 리전에 장애가 발생할 경우 시스템이 이를 즉시 인지하여 다른 리전으로 요청을 자동으로 우회합니다.

AWS 서비스 포터의 개념과 활용 이유
장애 발생 시 리전 간 라우팅을 처리하는 기법이 필요하며, 이를 구현하기 위해 AWS 서비스 포터(Service Porter)를 활용합니다.
서비스 포터는 리전 장애를 인지하고 트래픽을 우회시키는 등 서비스 안정성을 확보하는 핵심적인 역할을 수행합니다.

AWS 리소스 할당량의 개념과 필요성
AWS는 퍼블릭 클라우드 서비스로서 글로벌 인프라를 기반으로 서울 리전 내 데이터 센터와 서버를 운영합니다.
사용자가 생성하는 리소스에는 제한된 할당량이 존재하며, 이는 공유 자원을 효율적으로 관리하기 위한 필수적인 설정입니다.

AWS 쉐어드 테넌시와 리소스 제한
AWS 서울 리전의 서버는 여러 고객이 함께 사용하는 쉐어드 테넌시 방식으로 운영됩니다.
특정 사용자가 리소스를 독점하여 다른 사용자가 자원을 이용하지 못하는 상황을 방지하기 위해 서비스 할당량인 서비스 쿼터가 적용됩니다.

Amazon Bedrock 엔드포인트 할당량 확인 방법
Amazon Bedrock 공식 문서에서 모델별 추론 요청에 대한 리전별 할당량 정보를 확인할 수 있습니다.
모델 호출 시 발생하는 분당 추론 요청 제한 등 상세한 할당량 기준을 문서 내에서 조회할 수 있습니다.

Amazon Bedrock 서비스 할당량 및 조정 방법
Amazon Bedrock 서비스는 기능별로 할당량이 제한되어 있으며, 할당량 조정 가능 여부는 항목에 따라 다릅니다.
조정이 가능한 할당량은 AWS 서포트 팀에 요청하여 증설할 수 있습니다.
일부 서비스 할당량은 조정이 불가능한 제약 사항으로 설정되어 있습니다.

인스턴스 프로핑의 개념 및 역할
서울 리전의 추론 요청 할당량이 소진될 경우 다른 리전에서 처리가 가능하도록 지원합니다.
인스턴스 프로핑은 추론 수행 리전의 앞단에서 프론트엔드, 로드밸런서, 라우터 역할을 수행합니다.

인스턴스 프로비저닝을 통한 추론 서비스 가용성 확보
특정 리전의 추론 요청 할당량이 소진될 경우 다른 리전으로 요청을 전달하여 서비스 중단을 방지합니다.
글로벌 인프라를 활용한 이러한 기법을 통해 추론 서비스 제공 시 발생할 수 있는 장애를 최소화할 수 있습니다.

글로벌 애플리케이션을 위한 추론 프로파일 활용
추론 프로파일은 글로벌 애플리케이션 환경에서 여러 리소스를 효율적으로 사용하는 데 적합합니다.
해당 기능은 Invoke Model API와 Conversation API를 모두 지원합니다.
이러한 기능이 제공되는 배경에는 서비스 포터(Service Porter)의 역할이 있습니다.

배치 추론 실습 시나리오 설정
대기업 AI 엔지니어가 미디어 보도자료 대응 업무를 수행하는 상황을 가정하여 배치 추론 실습 시나리오를 구성합니다.

미디어 대응을 위한 신문 기사 정리 자동화
부정적인 언론 보도에 대한 반박 자료 준비 및 대응 업무를 수행합니다.
매일 아침 조간신문 기사를 자동으로 정리하기 위해 AWS EventBridge를 활용하는 방안을 제안합니다.

AI 인프라 교육 4일차: 배치 추론 워크플로우
오전 7시 주요 기사 발행 시간에 맞춰 스텝 펑션 워크플로우를 자동으로 실행하도록 스케줄링합니다.
배치 추론을 수행할 문서들에 대한 매니페스트 파일을 생성하는 작업을 첫 번째 단계로 진행합니다.

AI 인프라 교육: 배치 추론 파이프라인 구성
S3에 저장된 신문 기사 데이터를 불러와 매니페스트 파일을 생성하는 람다 함수를 실행합니다.
매니페스트 생성 완료 후 배치 추론을 수행하는 람다 함수를 호출합니다.
배치 추론 람다 함수는 Bedrock의 InvokeModel 또는 Converse API를 호출하여 추론을 진행합니다.

배치 추론 요청 및 상태 확인 과정
S3에 저장된 신문 기사와 매니페스트 파일을 참조하여 요약본 생성을 위한 배치 추론을 요청합니다.
배치 추론 작업이 완료될 때까지 약 5분간 대기합니다.
배치 추론의 진행 상태를 확인하기 위해 별도의 Lambda 함수를 호출하여 실행합니다.

Bedrock 배치 추론 작업 상태 확인 및 대기 프로세스
Bedrock API를 호출하여 배치 작업의 상태가 실행 중인지 완료되었는지 확인합니다.
작업이 완료되지 않고 실행 중인 경우 5분간 대기한 후 다시 상태를 확인하는 과정을 반복합니다.

배치 추론 결과 저장 및 자동화 구현
배치 추론 완료 후 LLM이 요약한 결과를 DynamoDB에 저장하는 Lambda 함수를 구현합니다.
EventBridge를 활용하여 매일 오전 7시에 배치 추론이 자동으로 실행되도록 설정할 수 있습니다.

요약 기술의 종류와 특징
추출 요약은 원문 내의 문장과 단어를 그대로 활용하여 내용을 정리하는 방식입니다.
추산 요약은 LLM이 원문을 분석한 뒤 새로운 문장으로 재구성하여 요약하는 방식입니다.
두 방식 모두 논문, 비즈니스 문서, 콜센터 대화 등 다양한 텍스트를 요약하는 데 활용될 수 있습니다.

AI 인프라 교육 4일차: 챗봇 아키텍처 구현
사용자 질문과 관련 콘텐츠를 S3 등에 저장하여 챗봇 아키텍처를 구성할 수 있습니다.
컨버스(Converse) API를 활용하여 사용자 질문과 저장된 콘텐츠를 결합한 프롬프트를 요청할 수 있습니다.
챗봇 기능 구현 시 메모리 추가 기능을 고려해야 합니다.

LLM 콘텍스트 윈도우의 구성 요소와 성능 영향
LLM의 콘텍스트 윈도우에는 시스템 프롬프트, 사용자 프롬프트, 이전 대화 내용, RAG 데이터, 도구 정보 및 메모리 등 다양한 데이터가 포함됩니다.
대화가 진행됨에 따라 입력과 출력이 윈도우에 누적되며, 전체 용량의 절반 이상을 차지할 경우 LLM의 응답 속도가 저하되거나 이전 맥락을 망각하는 현상이 발생합니다.

AI 모델의 메모리 유형 및 구분
데이터 손실을 방지하기 위해 중요한 정보를 별도 메모리에 저장하여 활용하는 방안이 제시되었습니다.
메모리는 크게 단기 메모리와 장기 메모리로 구분됩니다.
단기 메모리는 대화형 메모리라는 명칭으로도 불립니다.

단기 메모리와 장기 메모리의 차이점
단기 메모리는 동일한 세션 내에서 특정 턴의 콘텐츠를 임시로 저장하여 필요할 때 꺼내 쓰는 메모리입니다.
장기 메모리는 한 세션에서 주고받은 내용을 저장해 두었다가 이후 다른 세션에서 다시 불러와 사용할 수 있는 메모리입니다.

단기 메모리와 장기 메모리의 차이점 및 예제
단기 메모리는 현재 세션 내의 대화 내용을 참조하는 데 사용됩니다.
장기 메모리는 이전 세션에서 주고받았던 대화 내용을 기억하여 활용하는 데 사용됩니다.
호텔 및 비행기 예약 서비스를 제공하는 AI 챗봇 애플리케이션을 통해 메모리 활용 코드 예제가 제시되었습니다.

AI 챗봇을 통한 호텔 예약 데이터 관리
사용자가 AI 챗봇으로 예약한 호텔 정보와 예약 확인 번호가 DynamoDB에 저장됩니다.
화요일에 예약된 호텔 정보가 데이터베이스에 기록되어 관리됩니다.

AI 챗봇의 호텔 예약 정보 조회 과정
사용자가 이전에 수행한 호텔 예약 시점을 챗봇에게 질문합니다.
AI 챗봇은 DynamoDB에 저장된 과거 예약 데이터를 불러와 사용자에게 답변을 제공합니다.

비관리형 아키텍처 기반의 호텔 예약 시스템 구현
호텔 예약 정보 확인 기능을 비관리형 방식으로 직접 구현하는 코드 구조를 설명함.
프론트엔드와 백엔드 람다(Lambda) 함수, 그리고 Bedrock의 LLM을 호출하는 아키텍처로 구성됨.
사용자 ID를 활용하여 람다 함수를 호출하고 예약 정보를 처리하는 방식을 다룸.

Lambda 핸들러를 통한 유저 ID 추출 및 저장
Lambda 핸들러에서 JWT 인증 토큰을 파싱하여 유저 ID를 추출합니다.
추출된 유저 ID는 DynamoDB 테이블에서 대화 내용을 저장하기 위한 키 역할을 수행합니다.

키 기반 데이터베이스의 유저 ID 추출 과정
키-값 구조의 데이터베이스에서 유저 ID를 데이터 검색의 핵심 기준으로 설정하였습니다.
AWS Lambda 핸들러를 통해 JWT 인증 토큰 내 리퀘스트 콘텍스트에서 유저 ID를 추출하는 로직을 구현하였습니다.
추출된 유저 ID를 활용하여 이후 데이터 처리 작업을 수행합니다.

대화 기록 기반 메시지 선언 과정
Get Conversation History API를 호출하여 유저 ID를 기준으로 이전 대화 내용을 가져옵니다.
가져온 이전 대화 기록을 컨버세이션 메시지로 선언하고, 사용자의 새로운 메시지를 유저 메시지로 각각 정의합니다.

사용자 메시지 처리 및 저장 과정
새로운 사용자 메시지를 대화 목록에 추가하고 향후 활용을 위해 데이터베이스에 저장합니다.
메시지 저장을 위해 정의된 스토어 메시지 함수를 호출하여 데이터를 처리합니다.

유저 메시지 저장을 위한 DynamoDB 구현
유저 ID를 기준으로 이전 대화 내용을 30회까지 저장하도록 구현되었습니다.
메시지 저장을 위해 DynamoDB의 PutItem API를 호출합니다.
저장 항목에는 유저 ID, 타임스탬프, 메시지 역할, TTL 정보가 포함됩니다.

Bedrock Converse API 및 Nova Lite 모델 활용
스토어 메시지 함수를 사용하여 데이터를 저장하는 과정을 수행합니다.
Bedrock의 LLM 활용 시 시스템 프롬프트와 추론 구성을 지정합니다.
Converse API를 호출하여 Nova Lite 모델을 사용하는 방식으로 구현합니다.

AI 챗봇 에이전트 시스템 설정 및 데이터 저장
시스템 프롬프트는 AI 챗봇 에이전트가 대화형 응답을 제공하도록 설정되었습니다.
추론을 위해 Max Tokens 300과 Temperature 0.7 설정을 적용해야 합니다.
LLM의 응답 데이터는 추후 활용을 위해 DynamoDB에 저장해야 합니다.
모델 응답 API의 출력 메시지 중 첫 번째 텍스트 배열을 어시스턴트 메시지로 지정하여 DynamoDB에 저장하는 과정을 수행합니다.

AI 인프라 교육: HTTP 응답 및 DynamoDB 연동
클라이언트에게 HTTP 200 응답 코드와 함께 어시스턴트 메시지를 전달하는 코드 작성 방식을 설명하였습니다.
DynamoDB 테이블과 연동하여 컨버세이션 ID를 참조하고 메시지 기록을 가져오는 함수 기능을 안내하였습니다.

데이터 처리 방식 및 에이전트 코어 소개
대량의 메시지를 페이지 단위로 분할하여 처리하고 데이터베이스를 통해 메모리를 직접 관리하는 비관리형 설정 방식을 설명함.
내일 다룰 에이전트 코어는 이러한 메모리 관리 과정을 관리형으로 전환하여 쉽게 구현할 수 있도록 지원함.
AWS 환경에서 LLM 기반의 생성형 AI 애플리케이션이나 AI 에이전트를 개발할 때 해당 기능을 활용할 수 있음.

AI 에이전트 시스템 구현 방식 및 휴식 안내
에이전트 시스템은 비관리형 서비스와 관리형 서비스 중 선택하여 구현할 수 있습니다.
비관리형 구현 시 기존에 소개된 서비스들을 활용할 수 있으며, 관리형 구현을 지원하는 에이전트 코어는 내일 상세히 다룰 예정입니다.
강의 진행 중 휴식 시간을 갖습니다.

AI Infra 교육 4일차 실습 진행
오전 55분부터 교육 과정을 이어서 진행함.
실습 중 발생한 실수와 관련하여 공유가 필요한 내용을 추후 시연할 예정임.

AI Infra 교육 4일차 실습 및 휴식 안내
실습 1, 2, 3번 과정은 주피터 코드를 실행하고 내용을 확인하는 방식으로 진행됩니다.
실습을 빠르게 마무리한 후 15시 15분까지 휴식 시간을 갖습니다.

AI Infra 교육 4일차 실습 피드백 공유
교육생들의 피드백 결과 실습 난이도에 대한 의견이 극명하게 나뉘었습니다.
일부 교육생은 AI 내용이 어렵다는 의견을 제시하였고, 다른 교육생은 코드 실행 및 분석에 집중할 수 있었다는 반응을 보였습니다.
현재 머신러닝 과정은 주피터 노트북을 활용하여 코드를 실행하고 내용을 학습하는 방식으로 진행되고 있습니다.

AI Infra 교육 워크숍 진행 방식 논의
워크숍 진행 시 빈칸을 채우는 방식 대신 참가자가 직접 전체 내용을 작성하는 방식으로 변경하는 방안을 제안함.
참가자들의 의견을 수렴하기 위해 설문조사를 실시할 것을 제안함.

AI Infra 교육 실습 및 워크숍 안내
실습 1, 2, 3번은 기존 환경에서 진행 가능하나 실습 4번은 별도의 코드 실행이 필요하므로 실습 시간에 제공될 예정입니다.
교육 내용 보충을 위한 워크숍 진행 여부를 결정하기 위해 수강생 대상 서베이를 실시합니다.

AI 인프라 교육 실습 옵션 안내
교육 과정에서 선택 가능한 세 가지 실습 옵션을 제시하고 선호도를 조사함.
주피터 노트북을 활용한 간략한 실습 자료를 준비하여 제공할 예정임.

AI 인프라 교육 4일차 실습 안내
개발자 워킹 그룹에서 활용 가능한 OTB 도구의 사용을 제안함.
교육생들의 요청 사항을 반영하여 네이버 환경을 활용한 실습 환경을 구축함.

AI 인프라 교육 4일차 설문 조사 안내
QR 코드를 스캔하여 3분 내로 설문 조사에 참여해야 합니다.
설문 조사는 총 세 가지 문항으로 구성되어 있습니다.
결과는 다수결 원칙에 따라 반영될 예정입니다.

AI 인프라 교육 4일차 설문 결과 및 결선 투표
워크숍 설문 조사 결과 찬반 의견이 동일하게 나타났습니다.
참가자들에게 5분간의 숙고 시간을 부여한 후 결선 투표를 진행할 예정입니다.

AI 인프라 교육 실습 방식에 대한 의견
실습 진행 시 시나리오를 제공하고 빈칸을 채우는 방식이 처음부터 끝까지 직접 구현하는 것보다 효과적이라는 의견이 제시되었습니다.
강사는 해당 의견이 정답은 아니며 교육적 관점에서의 제언임을 밝히고 프레임워크 활용과 관련한 추가적인 논의를 이어갔습니다.

AI 인프라 교육의 실습 방향성 안내
머신러닝 프레임워크나 라이브러리의 세부 사용법은 공식 문서와 SDK 설명서를 참고하는 것이 더 효율적입니다.
교육 과정에서는 도구의 상세 기능보다는 이를 활용한 실제 구현 경험을 쌓는 데 집중할 예정입니다.

Amazon Bedrock 활용 및 교육 목적
Amazon Bedrock의 구체적인 사용 사례와 허용 범위는 AWS 공식 홈페이지의 고객 사례를 통해 확인할 수 있습니다.
교육 과정에서 Bedrock을 사용하는 주된 목적은 LLM을 실무에 활용하는 방법을 익히는 데 있습니다.
실제 프로젝트 수행 시에는 Bedrock에서 제공하는 API를 활용하여 필요한 기능을 구현할 수 있습니다.

AI 에이전트 코어 및 비관리형 클러스터 실습
프로젝트 에이전트 코어를 활용하여 API 호출을 통해 리소스를 생성하는 실습을 진행합니다.
비관리형 클러스터에서 에이전트를 직접 구성하고 API를 호출하는 방식은 교육 과정의 범위를 벗어나므로 다루지 않습니다.

AI 인프라 워크샵 실습 방향 제안
워크샵 과정에서 코드의 빈칸을 채우는 방식 대신 직접 전체 코드를 작성하는 실습을 도입할 수 있습니다.
파라미터를 직접 조정하며 모델의 반응을 확인하는 방식은 캐글과 같이 목표치를 달성하는 학습에 효과적입니다.

AWS 교육 방식 및 시간 제약에 대한 설명
코드 실행 위주의 교육 방식이 지닌 한계를 인지하고 있으나, 제한된 교육 시간으로 인해 불가피하게 진행하고 있습니다.
실제 고객사 대상 강의 시에도 시간 부족 문제로 인해 교육 과정을 단축하여 운영하고 있습니다.

AI 인프라 교육 실습 방식 및 방향성
강의 시간 제약으로 인해 일부 모듈을 생략하고 있으며, 개발자들이 AWS API 호출 동작을 직접 확인할 수 있도록 준비된 코드를 제공하고 있습니다.
수강생들의 능동적인 참여 요구를 반영하여 향후 실습 내용을 지속적으로 맞춤형으로 구성할 예정입니다.

AI 인프라 교육 4일차 결선 투표 진행
교육 과정 중 의견 수렴을 위해 2차 결선 투표를 실시합니다.
참가자는 제시된 선택지 중 하나를 선택해야 하며, QR 코드를 통해 투표에 참여할 수 있습니다.

AI 인프라 교육 4일차 결선 투표 결과
교육 과정 운영 방식에 대한 결선 투표 결과, 빈칸 채우기 형식으로 진행하자는 의견이 70%를 차지하였습니다.
참석자 13명 전원이 투표에 참여하여 최종 결과가 집계되었습니다.

AI 에이전틱 실습 워크샵 진행 안내
내일 진행될 워크샵은 에이전틱 AI 실습을 중심으로 구성될 예정입니다.
앱을 가장 먼저 개발하는 참가자에게는 별도의 상품이 제공됩니다.
워크샵의 타겟은 타 고객사에서 진행했던 사례를 동일하게 실습하는 방식으로 설정되었습니다.

AI 에이전트 실습 단계 및 구성 안내
채팅창을 통해 메시지를 보내면 에이전트 코어가 요청을 처리하여 응답하는 기본 에이전트 구현을 1단계로 진행합니다.
2단계에서는 메모리 기능을 추가하여 에이전트가 과거 대화 내용을 기억하고 대화를 이어갈 수 있도록 구성합니다.

AI 에이전트 구현 및 인증 검증 안내
에이전트가 클라이언트의 요청을 처리하고 그 결과를 메모리에 저장하는 이벤트가 정상적으로 구현되었는지 증명해야 합니다.
애플리케이션과 배포된 에이전트 간의 인증 구현을 통해 인증된 사용자만 접근할 수 있도록 설정해야 합니다.

AI Infra 교육 4일차 일정 및 휴식 안내
내일 교육에서는 에이전트를 호출하여 기능을 구현하는 실습을 진행할 예정입니다.
설문 조사 및 질의응답 시간을 고려하여 오후 3시 30분부터 교재 진도를 이어서 나갈 계획입니다.

AI 에이전트 실습 과제 안내
좌표결에서 결정된 사항에 따라 에이전트 코어를 활용한 실습을 진행합니다.
비용 계산 에이전트 또는 다른 에이전트 중 하나를 선택하여 직접 구현하는 과제가 주어졌습니다.

AI 인프라 교육 4일차 출석 확인 및 노쇼 안내
교육 과정의 출석률 80% 이상 유지 조건에 따라 결석자에 대한 출석 확인을 진행하였습니다.
김지환, 김태균, 박주환, 김도형, 김민석, 김민성, 민재홍 등 결석 인원은 노쇼로 처리되며, 이로 인해 수료가 불가능합니다.

AI 인프라 교육 4일차 모듈 및 RAG 실습 안내
교육 과정의 핵심 내용은 모듈 5번과 6번에서 다루어집니다.
수강생들의 의견을 수렴하여 교육을 신속하게 진행하고 종료 후 질의응답 시간을 확보할 예정입니다.
RAG(검색 증강 생성) 구현 경험 여부를 확인하고 관련 내용을 점검합니다.

RAG 구현 구성 요소 및 AWS 활용 방안
RAG의 주요 구성 요소와 구현 시 필요한 고려 사항을 설명함.
AWS 환경에서 RAG를 구현하는 방법으로 관리형과 비관리형 두 가지 옵션을 제시함.

AWS 기반 RAG 솔루션 구현 방식
AWS는 솔루션 구현 시 관리형과 비관리형 방식을 모두 제공하여 사용자에게 선택권을 부여합니다.
RAG 기술의 핵심 내용을 빠르게 설명한 뒤 각 구현 방식별 적용 방법을 안내합니다.

RAG 기술을 활용한 프롬프트 증강 원리
엔터프라이즈 조직의 지식 소스나 관리형 데이터베이스에서 사용자 프롬프트와 관련된 문서 청크를 검색합니다.
검색된 관련 정보와 사용자 프롬프트를 결합하여 프롬프트를 증강하는 과정을 수행합니다.

RAG의 정의 및 구현 파이프라인
RAG는 외부 컨텍스트를 LLM에 전달하는 과정을 의미합니다.
RAG 구현을 위해서는 인젝션 파이프라인을 구성하는 것이 필수적입니다.

RAG 구성을 위한 데이터 소스 유형
조직이 보유한 데이터 소스는 문서 리포지토리, 관계형 데이터베이스, 비관리형 데이터베이스 등 다양한 형태로 존재합니다.
가장 보편적인 사례인 문서 기반의 RAG(Retrieval-Augmented Generation) 구성을 전제로 데이터 활용 방안을 설명합니다.

머신러닝 전처리 과정에서의 데이터 클리닝
머신러닝 엔지니어링의 초기 단계인 전처리 과정에서 데이터 클리닝 작업이 필수적으로 수행됩니다.
데이터 클리닝은 문서 내 불필요한 헤더나 푸터와 같은 노이즈를 제거하여 데이터의 품질을 높이는 역할을 합니다.

데이터 전처리 및 청크 분할 과정
데이터 클리닝 단계에서 불필요하거나 중복된 데이터를 제거하고 문서 포맷을 통합하여 데이터의 일관성을 확보합니다.
큰 문서를 토큰 단위의 청크로 분할하여 처리 효율성을 높입니다.

RAG 구축을 위한 청킹 전략의 종류
문서의 구조나 단락 단위로 나누는 방법과 토큰 사이즈를 직접 지정하는 방식이 있습니다.
의미론적 연산을 통해 문맥이 구분되는 문장 단위로 분할하거나 계층적으로 쪼개는 전략을 활용할 수 있습니다.
RAG 시스템을 구성할 때 가장 먼저 고려해야 할 핵심 요소로 적절한 청킹 전략의 선택이 강조되었습니다.

AI 인프라 교육: 청킹 및 임베딩 전략
문서 처리 과정에서 효율적인 청킹 전략을 수립하는 것이 중요합니다.
청킹을 통해 분할된 문서는 다차원 배열 형태인 벡터로 변환하는 임베딩 과정을 거쳐야 합니다.

임베딩 모델 선택 및 메타데이터 관리 전략
임베딩 모델을 결정하는 것은 실습 과정의 두 번째 선택 사항입니다.
문서 정보를 지칭하는 메타데이터는 수동으로 생성하거나 랭체인 및 랭그래프 프레임워크를 활용하여 데이터 소스 단계에서 구성할 수 있습니다.

메타데이터 추출 및 벡터 데이터베이스 저장
메타데이터는 데이터 검색의 효율성을 높이기 위해 작성 및 활용됩니다.
임베딩된 청크와 사전에 생성한 메타데이터를 최종적으로 데이터베이스에 저장합니다.
RAG(검색 증강 생성) 시스템 구성 시 문서 처리를 위해 벡터 데이터베이스를 주로 사용합니다.

RAG 시스템의 구성 및 검색 과정
관계형 데이터베이스를 사용할 경우 PGVector와 같은 벡터 확장 기능을 활용하여 데이터를 처리합니다.
사용자가 입력한 프롬프트는 임베딩 과정을 거쳐 벡터화됩니다.
벡터화된 사용자 프롬프트와 벡터 데이터베이스에 저장된 문서 청크 간의 유사도를 비교하여 검색을 수행합니다.

RAG(검색 증강 생성)의 개념 및 작동 원리
유사도 검사나 거리 기반 분석을 통해 관련 문서를 검색합니다.
검색된 문서와 프롬프트를 결합하여 LLM에 전달하는 것이 RAG의 핵심 개념입니다.
지식 기반 하이드레이션(Knowledge-based Hydration)은 이러한 데이터 처리 과정을 상세히 설명하는 용어입니다.

RAG 구축을 위한 하이드레이션 과정 및 문서 전처리
RAG 모델의 외부 지식 기반을 구축하고 업데이트하는 과정을 하이드레이션이라고 정의합니다.
문서 수집 단계에서는 데이터를 수집하여 저장소에 보관합니다.
문서 정제 단계에서는 중복 제거, 헤더 및 푸터 삭제, 노이즈 제거를 수행합니다.
AI 모델의 효율적인 처리를 위해 수집된 문서를 마크다운 형식으로 변환합니다.

청크 전략의 개념 및 픽스드 사이즈 청크
텍스트를 미리 지정한 토큰 단위로 균일하게 분할하는 픽스드 사이즈 청크 전략을 설명함.
픽스드 사이즈 청크는 구현이 간단하다는 장점을 가짐.

AI 인프라 교육: 텍스트 청크 오버랩 기법
텍스트를 분할하여 처리할 때 문장이 중간에 끊기는 문제를 방지하기 위해 오버랩 기법을 사용합니다.
첫 번째 청크와 두 번째 청크가 일부 내용을 공유하도록 구성하여 데이터 처리의 효율성과 정확성을 높입니다.

청킹 전략의 종류와 특징
중복 영역을 설정하는 방식은 픽스드 사이즈 청킹 구현 시 의미 단위 해체 가능성을 보완하는 역할을 합니다.
스트럭처 어웨어 청킹은 마크다운, HTML, 라텍스 등 문서 구조를 표현하는 태그를 활용하여 데이터를 처리합니다.

명확한 구조를 갖춘 문서 기반의 RAG 구축
마크다운 문법을 활용하여 타이틀, 인트로덕션, 섹션, 컨클루전 등 명확한 구조로 문서를 구성합니다.
구조화된 문서를 바탕으로 RAG(검색 증강 생성) 작업을 수행합니다.

구조화된 문서의 청킹 전략과 주의사항
블로그, 기술 매뉴얼, 법률 문서와 같이 구조가 명확한 문서는 구조 인식 청킹(Structure-aware chunking) 방식을 적용해야 합니다.
구조 인식 청킹을 사용할 경우 문서마다 청크 사이즈가 달라질 수 있다는 점을 고려해야 합니다.
구조가 명확하지 않은 문서에 해당 방식을 적용하면 유사 문서 검색 성능이 저하될 가능성이 있습니다.

시맨틱 청킹의 개념 및 수행 방식
시맨틱 청킹은 텍스트 내 의미 경계를 계산하여 데이터를 분할하는 방식입니다.
인공지능, 머신러닝, 딥러닝과 같이 의미가 구분되는 구간을 감지하여 청킹을 수행합니다.
의미 기반의 청킹을 구현하기 위해서는 문장을 벡터화하는 과정이 선행되어야 합니다.

코사인 유사도 기반 청킹의 특징과 비용
코사인 유사도를 활용한 청킹 방식은 문맥 단절을 효과적으로 감지할 수 있어 의미 전달이 중요한 보고서나 진단서 등에 적합합니다.
청크 사이즈가 가변적이며 유사도 계산을 위한 연산 과정에서 컴퓨팅 파워 사용에 따른 비용이 발생합니다.

계단식 청킹(Cascading Chunking)의 개념 및 작동 방식
계단식 청킹은 설정된 청크 사이즈를 기준으로 데이터를 분할하는 방식입니다.
청크가 지정된 벡터 크기 내에 포함될 경우 추가로 쪼개지 않습니다.
청크 사이즈가 기준치를 초과하는 경우, 설정된 토큰 단위에 맞춰 데이터를 재분할합니다.

계층적 문서 분할을 통한 청크 생성 방식
문서의 의미를 유지하면서 100 토큰 이내의 다양한 크기로 청크를 재귀적이고 계층적으로 분할합니다.
청크 사이즈 기준을 충족하는 패러그래프는 추가 분할 없이 그대로 유지합니다.

RAG 구성을 위한 청크 분할 전략 및 임베딩
문서의 특성에 따라 고정 크기, 구조 기반, 시맨틱, 재귀적 분할 등 다양한 청크 분할 전략을 선택할 수 있습니다.
청크 분할 시 설정에 따라 데이터 간 오버랩이 발생할 수 있습니다.
임베딩은 데이터를 벡터화하여 처리하는 과정을 의미합니다.

임베딩 모델을 활용한 문서 벡터화 및 검색
문서를 벡터화하는 과정인 임베딩을 수행하기 위해서는 별도의 임베딩 모델이 필요합니다.
실무에서는 임베딩 모델을 통해 문서를 벡터화한 후 코사인 유사도를 계산하여 유사한 문서를 검색합니다.

임베딩 모델을 활용한 벡터 데이터베이스 구성 및 검색
데이터를 분할한 뒤 임베딩 모델을 통해 벡터화하여 데이터베이스에 저장합니다.
저장된 벡터 데이터 간의 거리와 유사도를 계산하여 관련 정보를 검색합니다.
검색 시 상위 K개의 결과를 추출하며, K값은 사용자가 직접 설정할 수 있습니다.
임베딩 모델이 지원하는 구체적인 모델 종류에 대한 설명이 이어집니다.

Bedrock 임베딩 모델 지원 현황
Bedrock에서 지원하는 임베딩 모델은 제한적입니다.
Amazon의 임베딩 모델 중 Titan 모델을 사용할 수 있습니다.

델타 모델의 임베딩 유형 및 사양 확인
델타 모델은 텍스트 임베딩과 멀티모달 임베딩을 모두 지원하며 텍스트 임베딩에 최적화된 형태를 갖추고 있습니다.
모델 사양에서 입력 토큰의 최대치, 벡터 사이즈, 주요 활용 사례 등을 확인할 수 있습니다.

AWS 한국어 지원 임베딩 모델 안내
현재 AWS 환경에서 한국어를 지원하는 임베딩 모델로 Cohere 솔루션을 활용할 수 있습니다.

임베딩 모델 활용 및 메타데이터의 중요성
한국어를 지원하는 임베딩 모델을 활용하여 실습을 진행할 수 있습니다.
검색 성능을 향상하기 위해 벡터 데이터와 함께 메타데이터를 활용하는 것이 필수적입니다.

지식 소스 데이터의 메타데이터 추출 방식
지식 소스 데이터에서 메타데이터를 추출할 때는 수동 주입 방식이 아닌 소스 문서에서 직접 추출하는 코드를 구현해야 합니다.
정제된 데이터 상태에서 메타데이터를 추출하는 과정이 필수적으로 요구됩니다.

지식 소스 데이터의 임베딩 및 벡터 저장 구현
지식 소스 데이터를 임베딩 모델을 통해 처리한 후 벡터 데이터베이스에 저장하는 과정을 코드로 구현합니다.
지금까지 설명한 단계들을 종합하여 데이터 임베딩부터 벡터 저장까지의 전체 구현 흐름을 완성합니다.

AI 인프라 교육: 인덱싱 및 검색 전략
사용자 프롬프트를 통한 유사 문서 검색을 위해 적절한 인덱싱 전략을 수립해야 합니다.
유사 문서 검색 방식은 크게 KNN과 ANN으로 구분됩니다.

정확 검색 방식의 개념 및 특징
정확 검색 방식은 벡터 DB에 저장된 모든 문서 청크와 사용자 프롬프트 간의 거리를 계산하여 가장 유사한 최근접 이웃을 추출합니다.
이 방식은 RAG 구성 요소인 모든 벡터를 전수 조사하는 과정을 거치므로 연산량이 많다는 특징이 있습니다.

대규모 데이터셋을 위한 근사 검색 알고리즘
소규모 데이터셋과 달리 대규모 데이터셋에서는 전체 탐색 시 시간이 과도하게 소요됩니다.
효율적인 유사 벡터 탐색을 위해 정확도를 일부 희생하는 대신 속도를 크게 향상시킨 근사 검색 알고리즘인 ANN(Approximate Nearest Neighbor)을 사용합니다.

그래프 기반 데이터베이스의 활용과 검색 성능
그래프 기반 데이터베이스는 데이터 간의 연결 강도와 방향성을 구현하는 데 특화되어 있습니다.
SNS의 친구 관계와 같이 데이터 간의 관계를 정의해야 하는 환경에서 그래프 데이터베이스를 활용할 수 있습니다.
효율적인 검색 성능을 확보하기 위한 방안으로 그래프 기반의 HNSW 알고리즘이 언급되었습니다.

알고리즘의 개념과 신세계 백화점 예시
알고리즘의 개념을 설명하기 위해 신세계 백화점 본점을 방문하여 푸마 매장에서 러닝화를 구매하는 과정을 예시로 제시함.
백화점 내 본관과 신관으로 나뉘어 있는 구조를 통해 특정 목적지에 도달하기 위한 경로 탐색 과정을 설명함.

백화점 내 브랜드 매장 위치 탐색 방식
신관 1층부터 4층까지 연결된 층별 구조를 확인하고 특정 브랜드가 위치한 층으로 이동합니다.
아웃도어 층인 7층에 방문하여 아식스, 나이키, 아디다스 등 다양한 매장을 확인합니다.
연결된 매장 정보를 활용하여 푸마와 같은 특정 브랜드 매장을 탐색합니다.

그래프 기반 유사도 검색의 특징 및 활용
그래프 연결 관계를 활용하여 유사한 데이터를 빠르게 검색하는 기술입니다.
실시간 이미지 검색 및 AI 챗봇의 유사 문장 검색 분야에 주로 활용됩니다.
빠른 속도와 높은 정확도를 제공하지만, 모든 연결 정보를 메모리에 상주시켜야 하는 메모리 점유 부담이 존재합니다.

메모리 저장량과 공간 분할 기반 인덱스
1빌리언 규모의 128차원 데이터는 약 1.4테라바이트의 메모리 저장 공간을 필요로 합니다.
공간 분할 기반 인덱스는 쇼핑몰의 카테고리 분류 체계와 유사하게 데이터를 구조화하여 관리합니다.

공간 분할 인덱스의 개념 및 특징
공간 분할 인덱스는 벡터화된 문서를 클러스터링하여 버킷 단위로 분할한 뒤 쿼리 시 일부 버킷만 검색하는 방식입니다.
해당 방식은 필요한 메모리 용량을 절감할 수 있다는 장점이 있습니다.
인덱스 기반 검색 방식과 비교할 때 쿼리 성능과 정확도는 다소 낮아질 수 있습니다.

데이터 처리 및 양자화 기술의 이해
대량의 데이터 처리가 필요한 쇼핑몰이나 데이터 분석 분야에 적합한 기술적 특성을 설명함.
양자화는 차원을 축소하여 벡터 인덱싱 효율을 높이는 기술임.
양자화를 통해 벡터를 압축하면 메모리 사용량을 줄일 수 있음.

벡터 스토어 인덱싱 및 하이브리드 검색
모바일 및 IoT 환경에서는 정확도 손실을 감수하는 인덱싱 방식이 적합합니다.
사용하는 벡터 스토어의 종류에 따라 지원되는 인덱싱 방식이 달라집니다.
인덱싱 기법 중 하나로 하이브리드 검색 방식이 존재합니다.

RAG 시스템의 검색 기법 및 오케스트레이터 역할
의미 검색과 키워드 검색을 결합하여 벡터 데이터베이스에서 정보를 검색하는 하이브리드 검색 기법을 활용합니다.
구조화된 도메인 데이터와 의미론적 검색을 통합하여 검색의 정확도를 높입니다.
RAG 시스템에서 사용자 프롬프트와 검색된 문서 청크를 관리하고 조율하는 오케스트레이터 계층이 필요합니다.

LLM 컨텍스트 준비 및 리랭킹 과정
검색된 문서 청크 중 우선순위를 결정하는 리랭킹 과정을 통해 LLM에 전달할 최적의 컨텍스트를 구성합니다.
컨텍스트 준비 단계는 람다 함수를 활용한 코드 구현이나 RAG 오케스트레이터 계층을 통해 수행됩니다.

AWS 기반 RAG 구현 솔루션 및 지원 서비스
AWS는 LangChain, LlamaIndex, Hugging Face 등 오픈소스 프레임워크를 활용한 RAG 구현을 지원합니다.
벡터 검색이 가능한 RDS for PostgreSQL 확장 기능과 MemoryDB, DocumentDB 등 다양한 데이터베이스 서비스를 제공합니다.
LLM 서비스로는 Amazon Bedrock을 활용할 수 있습니다.

Amazon Bedrock 및 SageMaker 활용 안내
Amazon Bedrock은 LLM을 API 형태로 제공하며, SageMaker 인스턴스를 통해서도 파운데이션 모델을 활용할 수 있습니다.
관리형 RAG 구성을 구현할 때는 Amazon Bedrock Knowledge Bases를 활용할 수 있습니다.
Amazon Bedrock Knowledge Bases는 RAG 구성에 필요한 요소들을 통합하고 오케스트레이션하는 기능을 제공합니다.

Bedrock Knowledge Base와 데이터 동기화의 중요성
Bedrock Knowledge Base를 활용하여 RAG(검색 증강 생성)를 구현할 수 있습니다.
기업 데이터 소스와 벡터 DB 간의 지속적인 동기화가 필수적으로 요구됩니다.

LLM 지식 동기화 및 데이터베이스 설정
LLM의 지식 단절 문제를 해결하기 위해 주기적인 데이터 동기화 기능이 필요합니다.
데이터베이스 설정을 위해서는 데이터 소스 확보가 필수적인 선행 조건입니다.

AI 인프라 구축을 위한 권한 및 구성 요소
벡터 데이터베이스 활용을 위해 적절한 데이터베이스 선택이 필요합니다.
나이즈 베이스 구현 시 S3 및 Bedrock에 접근하기 위한 IAM 역할 설정이 필수적입니다.
Bedrock 기반의 나이즈 베이스를 구성하려면 LLM에 대한 접근 권한이 요구됩니다.

Knowledge Base 생성 및 API 활용 방법
파운데이션 모델 액세스 권한을 확보한 후 API를 통해 Knowledge Base를 생성할 수 있습니다.
Create Knowledge Base API 호출 시 IAM 역할과 Knowledge Base 구성을 위한 설정 정보가 포함되어야 합니다.
Knowledge Base 구성 단계에서 벡터 형태의 데이터 저장 방식을 설정할 수 있습니다.

임베딩 모델 설정 및 멀티모달 데이터 처리
RAG 시스템에서 사용할 임베딩 모델을 지정하고 모델별로 지원하는 벡터 타입과 차원을 설정합니다.
특정 임베딩 모델은 바이너리 형태의 1024 차원을 지원하며 이에 맞춰 설정을 구성합니다.
지식 기반 데이터에 멀티모달 데이터가 포함된 경우 추출된 데이터를 별도로 저장하는 기능을 지원합니다.

오픈서치 서버리스 기반 벡터 DB 설정
오픈서치 서버리스를 벡터 데이터베이스로 활용하기 위한 스토리지 구성을 확인하였습니다.
지식 베이스를 생성하고 임베딩 모델을 설정하는 과정을 완료하였습니다.

데이터 소스 커넥터 구성 및 API 호출
임베딩 설정과 데이터 저장 위치 지정에 이어 데이터 소스 구성 단계를 진행합니다.
데이터 소스 커넥터 구성 시 Create Data Source API를 호출하여 설정을 완료합니다.

데이터 소스 구성 및 청킹 전략 안내
데이터 소스 구성 시 S3, 웹 URL, CSS, SharePoint 등을 지원합니다.
지원되는 청킹 전략에는 고정 크기(Fixed size), 계층적(Hierarchical), 의미 기반(Semantic) 방식이 있으며, 이미 청킹된 데이터를 위한 'None' 옵션도 제공합니다.

AI 인프라 API 호출 및 설정 파일 구성
Redshift와 커슨(Cursor)을 지원하는 API 호출 방식을 설명함.
데이터 소스 설정 및 베이스 ID를 포함한 구성 파일을 JSON 형식으로 작성하여 사용함.
S3 버킷 정보와 버킷 소유자 정보를 JSON 구성 파일에 명시해야 함.

AI 인프라 교육: 커넥터 및 청킹 전략 설정
S3 내 특정 파일을 선택하여 커넥터를 구성하는 방법을 설명함.
벡터 인젝션 설정에서 고정 크기 및 토큰 단위의 청킹 전략을 정의함.
최대 벡터 토큰 설정과 20%의 오버랩 비율을 적용하여 데이터를 분할함.

데이터 동기화 구현 및 자동화 방안
데이터 동기화를 위해 Stripe API를 호출하는 작업을 수행할 수 있습니다.
해당 작업은 일회성으로 실행되므로 지속적인 동기화를 위한 추가 로직 구현이 필요합니다.
EventBridge를 활용하여 API를 주기적으로 호출하는 방식을 고려할 수 있습니다.

S3 이벤트 기반 데이터 동기화 및 데이터베이스 소개
S3 버킷에 새로운 문서가 업로드될 때 발생하는 이벤트를 활용하여 추가적인 로직을 호출하고 데이터를 동기화할 수 있습니다.
지식 기반 시스템에서 활용 가능한 데이터베이스 및 스토리지 옵션에 대한 학습을 진행합니다.

벡터 데이터베이스 지원 옵션 및 활용 사례
나이즈 베이스(Knowledge Base)에서 지원하는 벡터 데이터베이스로 PostgreSQL의 pgvector, OpenSearch Serverless, S3 벡터 등을 활용할 수 있습니다.
관계형 데이터베이스에 벡터 RAG를 구현하거나 OpenSearch 비용을 최적화하기 위해 S3 벡터와 같은 다양한 옵션을 선택할 수 있습니다.

AI 인프라 교육: 데이터베이스 및 벡터 검색 옵션
PostgreSQL은 pgvector를 활용하여 RAG(검색 증강 생성) 기능을 지원하는 관계형 데이터베이스입니다.
OpenSearch Serverless는 JSON 문서를 데이터 모델로 사용하며 자동 수집, 벡터 검색, 규모 조정 및 비용 효율성을 제공합니다.
데이터베이스별 인덱싱 기능은 제공되는 옵션 내에 포함되어 있습니다.

OpenSearch의 인덱싱 알고리즘 지원 및 선택
OpenSearch는 그래프 기반 인덱싱과 IVF 기반의 공간 분할 인덱싱을 모두 지원합니다.
사용자는 인덱싱 시 그래프 기반 방식과 버킷 기반 방식 중 하나를 직접 선택하여 설정할 수 있습니다.

RAG 구성을 위한 벡터 데이터베이스 선정 기준
벡터 데이터베이스는 인덱싱 방법, 거리 지표, 지연 시간, 최대 처리 용량 등 다양한 기능적 차이를 제공합니다.
RAG 시스템 구축 시 데이터 소스의 특성에 따라 적합한 벡터 데이터베이스를 선택해야 합니다.

RAG 시스템 구성 요소 및 관리 도구
구현 분야에 따라 인덱싱 방식과 문서 분할 전략을 고정 크기 또는 시멘틱 방식으로 선택하여 RAG를 구성합니다.
관리형 서비스인 나이즈 베이스(Knockbase)를 활용하여 RAG 구성을 간편하게 관리할 수 있습니다.

오케스트레이터 계층을 위한 API 구성
RAG의 벡터 DB 검색을 수행하는 리트라이브(Retrieve) API와 외부 호출을 담당하는 인보크(Invoke) API를 통해 오케스트레이터 계층을 구성합니다.

RAG API 호출 및 응답 생성 과정
오케스트레이터는 사용자 질문과 지식 베이스 ID를 결합하여 관련 데이터를 검색하고 인용을 포함한 자연스러운 응답을 생성합니다.
사용자 질문과 지식 베이스 ID를 활용한 Retrieve and Generate API 호출 함수를 코드로 구현하여 설명합니다.

LLM 기반 RAG API 호출 및 실습 안내
LLM의 ARN 정보와 Retrieve Generate API를 호출하여 응답을 받는 과정을 설명함.
사용자 프롬프트와 Knowledge Base ID를 활용한 검색 및 출력 구조를 확인함.
모듈 1번까지의 이론 설명을 마친 후 남은 시간 동안 실습을 진행할 예정임.

Bedrock과 오픈소스 프레임워크의 연동 및 비교
Bedrock에서 제공하는 관리형 기능 외에도 오픈소스 프레임워크를 비관리형으로 직접 사용할 수 있습니다.
AI 에이전틱 프레임워크와 Bedrock을 연결하는 방법을 통해 각 방식의 장단점을 비교합니다.

오픈소스 프레임워크와 Bedrock API의 특징 비교
오픈소스 프레임워크는 특정 모델의 통합과 소스 코드 수정이 자유로워 커스터마이징이 용이하지만 관리 포인트가 증가합니다.
Bedrock API는 제공되는 기능을 호출하는 방식이므로 사용이 간편한 반면 소스 코드 접근이 제한되어 커스터마이징 측면에서는 상대적으로 약점이 있습니다.

AI 인프라 구축을 위한 프레임워크 및 LLM 조합
애플리케이션 개발 속도를 높이기 위해 오픈소스 프레임워크와 Amazon Bedrock 기반의 LLM을 결합하는 방안을 제시함.
추후 클로드(Claude)와 같은 모델을 활용하여 Amazon Bedrock 기반의 LLM 환경을 더욱 고도화할 수 있음.
랭체인(LangChain)을 활용한 프레임워크 구성 및 LLM 연동 방식을 설명함.

LangChain에서 Bedrock 활용 방법
LangChain 프레임워크에서 Bedrock을 사용하기 위해 Bedrock Converse 프로바이더를 활용합니다.
Chat 모델 호출 시 모델 프로바이더를 Bedrock Converse로 설정하여 연동을 수행합니다.

LangChain을 활용한 API 연동 및 프롬프트 구성
LangChain 라이브러리의 프롬프트 모듈을 사용하여 시스템 프롬프트와 사용자 프롬프트를 호출하는 방식을 설명함.
주문 작업을 수행하기 위해 드락(Drak) API를 연결하는 과정을 안내함.
LangChain의 주요 구성 요소에 대해 교육생들의 이해도를 확인하고 반응을 살핌.

LangChain을 활용한 에이전틱 워크플로우 구성
LangChain을 사용하여 LLM 기반의 간단한 워크플로우 형태인 에이전틱 워크플로우를 구현할 수 있습니다.
순차적으로 작업을 수행하는 일자형 워크플로우 수준까지 프레임워크를 통해 구현이 가능합니다.
솔루션 구현 과정에서 필요한 LLM 구성 요소의 중요성이 강조되었습니다.

RAG와 에이전트의 역할 및 외부 도구 활용
RAG는 조직 내 문서나 데이터베이스를 AI와 결합하여 정보를 제공하는 역할을 수행합니다.
데이터베이스나 문서 저장소에 없는 정보에 대응하기 위해 외부 도구를 호출하는 에이전트와 도구 지원이 필요합니다.

LangChain의 구성 요소 및 체인 동작 방식
LangChain은 외부 도구와 리소스를 불러와 구성할 수 있는 기능을 제공하며 메모리 기능을 지원합니다.
선언된 리소스들을 체인으로 묶으면 각 단계가 연계되어 순차적으로 동작하는 구조를 갖습니다.

프롬프트 템플릿을 활용한 API 호출 과정
시스템 프롬프트 템플릿 내에 사용자 프롬프트의 언어 값을 포함하도록 선언합니다.
선언된 프롬프트 템플릿을 기반으로 챗 프롬프트 템플릿을 구성합니다.
최종적으로 구성된 프롬프트 템플릿을 사용하여 API를 호출합니다.

AI 인프라 교육: 체인, 인덱스 및 RAG 구현
체인은 워크플로우를 실행하고 구성 요소를 연결하여 AI 에이전트를 구현하는 역할을 수행합니다.
인덱스는 RAG(Retrieval-Augmented Generation)의 핵심 요소로서 소스 문서에서 데이터를 효율적으로 가져올 수 있도록 지원합니다.
문서 처리를 위한 청킹 모듈과 대화 맥락 유지를 위한 메모리 기능이 함께 제공됩니다.

랭체인을 활용한 에이전트 아키텍처 구현
LLM과 외부 도구를 결합하여 에이전트를 구성하는 방법을 설명함.
모듈 3에서 다룬 아키텍처를 랭체인 라이브러리를 사용하여 실제로 구현함.

Bedrock 기반 챗봇 프롬프트 및 체인 구성
Bedrock 모델을 선언하고 시스템 메시지와 대화 기록을 포함한 프롬프트 템플릿을 생성하였습니다.
사용자 질문을 동적으로 처리하기 위해 퀘스천 변수를 설정하였습니다.
프롬프트와 모델을 연결하는 파이프라인 체인을 구성하였습니다.
이전 대화 데이터 저장을 위해 DynamoDB를 활용하였습니다.

LangChain의 RunnableWithMessageHistory 모듈 활용
LangChain에서 제공하는 RunnableWithMessageHistory 모듈을 사용하여 대화 기록을 효율적으로 관리하고 업데이트합니다.
DynamoDB의 세션 테이블을 활용하여 세션 ID를 기준으로 대화 기록을 저장하고 관리합니다.

챗봇 서비스의 세션 아이디 기반 히스토리 관리
세션 키와 히스토리 키를 사용하여 사용자별 데이터를 저장하고 관리합니다.
클라이언트가 챗봇 서비스를 이용할 때 세션 아이디를 통해 사용자별 대화 맥락을 구분합니다.
컨피거를 선언한 후 체인 빌드 히스토리를 활용하여 세션 아이디 기반의 리포트 구성이 가능합니다.

AI 인프라 교육 4일차 마무리 및 향후 계획
세션 ID를 활용하여 저장된 대화 내용을 불러오고 주고받는 RAG(Retrieval-Augmented Generation) 구현 방식을 설명하였습니다.
금일 모듈 6번까지의 교육 과정을 모두 마쳤습니다.
내일은 생성형 AI 애플리케이션 구축과 관련된 내용을 다룰 예정입니다.

AI Infra 교육 4일차 강의 계획
리스폰스 AI 구현을 위한 가드 설정 방법을 설명합니다.
에이전틱 AI의 개념과 오픈소스 프레임워크 및 Bedrock 에이전트 코어를 다룹니다.
에이전트 코어 워크숍을 통해 실습을 진행합니다.

AI Infra 교육 4일차 실습 안내
시스템 4.1 및 4.2 항목에 대한 실습을 진행해야 합니다.
4.1 실습은 Amazon Bedrock의 Knowledge Bases를 활용하여 RAG(검색 증강 생성)를 구축하는 과정입니다.
Retrieve API와 Generate API를 호출하여 RAG의 정상 동작 여부를 확인해야 합니다.

AI Infra 교육 실습 3·4번 안내
실습 3번과 4번의 4.1 항목에서 이미 구성된 RAG(Retrieval-Augmented Generation) 환경을 확인할 수 있습니다.
관리자 페이지를 통해 RAG 구성 정보를 조회하고 모듈 5번에서 다룬 내용을 직접 검토할 수 있습니다.

RAG 쿼리 및 API 호출 실습
RetrieveGenerate API를 호출하여 RAG 쿼리를 수행하는 과정을 설명합니다.
API 호출 시 'number_of_regions' 파라미터를 설정하여 가장 관련성이 높은 문서 3개를 검색하도록 지정합니다.

AI Infra 교육 4일차: RAG 구성 실습
응답 결과 출력 함수 및 관련 정보 확인 방법을 학습합니다.
LangChain을 활용하여 사용자 맞춤형 RAG를 구성하는 실습을 진행합니다.
Retrieve API 사용법과 응답에서 유사도 기반의 정보를 추출하는 과정을 다룹니다.

AI Infra 교육 4일차 실습 안내
색도 추출 및 사용자 정보와 검색 결과를 결합한 프롬프트 구성 실습을 진행합니다.
LangChain 모듈을 활용하여 LLM과 리트리브 API로 검색된 문서를 통합하는 커스텀 데이터 기반 실습을 수행합니다.

AI 인프라 교육 4일차 마무리 및 일정 안내
금일 오후 5시 30분까지 실습 4.1과 4.2 과정을 진행합니다.
내일 오전 9시 30분부터 평가 모듈을 포함한 교육 과정을 이어갈 예정입니다.

AI 인프라 교육 4일차 실습 안내
실습 중 랩 리밋(Lab Limit)이 발생한 경우 기존 실습을 종료하고 약 10분 정도 대기해야 합니다.
실습 진행 중 발생하는 질문 사항은 언제든지 문의할 수 있습니다.

LLM 기반 리트리버 테이블의 데이터 가변성
LLM이 지속적으로 데이터를 조립하여 전달하는 과정에서 리트리버 테이블의 내용이 변경될 수 있습니다.
데이터가 매번 달라질 수 있다는 점은 해당 기술을 활용할 때 자주 제기되는 질문 사항입니다.

AI 인프라 교육 워크샵 자료 공유
희망자를 위해 자비TV 워크샵 촬영본을 별도로 보관하였습니다.
기존 교육 과정 종료 후 데이터를 삭제하는 관례와 달리 실습을 위해 해당 자료를 유지하였습니다.